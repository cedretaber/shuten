# AGENTS.md

コーディングエージェント（Claude Code、qwen、その他）への指示。人間の開発者にも同じルールが適用される。
リポジトリの情報はここに書かず `docs/` に置く。必要なときに参照すること。

## 作業を始める前に

1. `README.md` で概要と現在の状態を確認する。
2. 仕様の正本は `docs/spec/mvp-spec.md` のみ。設計・実装・テストの判断はすべてここに基づく。
3. 実装に着手する前に `docs/reference/invariants.md` を読む。仕様から導いた不変条件と対象外の一覧で、
   実装の都合で崩してはならない。
4. コードを書くときは `docs/reference/conventions.md` の規約に従う。
5. 設計の背景が必要なら `docs/decisions/`、検証の手順や生データが必要なら `docs/experiments/` を見る。

## 守ること

- 仕様書の「合意した機能範囲」を勝手に広げない。初期値（分割長 1,500 字など）は調整可能だが、機能は増やさない。
- 仕様書 13 節の未決事項はエージェントが独断で確定しない。選択肢と推奨を示し、ユーザーの決定を待つ。
- 不変条件を変えたいときは、コードを変えるのではなく仕様書の改訂を提案する。
- 仕様の解釈に迷ったら、推測で進めずコメントかドキュメントに疑問点を残す。
- 作業の完了前に `pnpm check` を通す。Windows で未確認なら、その旨を明記する。
- 公開リポジトリである。個人情報、機器固有の値（IP アドレス、ユーザー名を含むパス）、実原稿をコミットしない。

## 言語

- ドキュメント、コミットメッセージ、PR、Issue、コード内コメントは日本語。
- 識別子（変数名・関数名・ファイル名）は英語。
- 他のエージェントに渡すスペックは、そのエージェントが最も得意とする言語で書いてよい。

## コミット

- `main` に直接コミットせず、作業ごとにブランチを切る。
- コミットメッセージは日本語。1 行目に変更の要約、必要なら空行を挟んで理由を書く。
- 仕様書を改訂したら改訂記録（15 節）に追記し、同じコミットに含める。

## コマンド

```sh
pnpm install          # 依存の導入
pnpm check            # typecheck + lint + test。完了前に必ず通す
pnpm dev              # server と web を同時起動
```

その他のコマンドは `docs/reference/conventions.md`。
