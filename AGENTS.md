# AGENTS.md

AI エージェント（Claude Code、qwen、その他）がこのリポジトリで作業するときの共通指針。
人間の開発者にも同じルールが適用される。

## プロジェクト概要

**朱点（shuten）** は、日本語の小説から誤字・脱字と不自然な日本語を検出する校正ツール。
LM Studio 上のローカル LLM を使い、Windows 上の単一ユーザー環境で動作する
Web UI + ローカルバックエンドの構成を想定している。

- 仕様の正本は `mvp-spec.md` のみ。設計・実装・テストの判断はすべてここに基づく。
- 仕様書は「合意した機能範囲」と「実装時に調整する初期案」を区別している。
  初期値（分割長 1,500 字など）は調整可能だが、機能範囲は勝手に広げない。
- 現在は実装前。技術スタック（UI・バックエンド・保存形式）は未決（仕様書 13 節）。

## 言語

- ドキュメント、コミットメッセージ、PR、Issue、コード内コメントは日本語で書く。
- 識別子（変数名・関数名・ファイル名）は英語。
- 他のエージェントに渡すスペックは、そのエージェントが最も得意とする言語で書いてよい。

## 仕様書から導かれる不変条件

以下は仕様書で確定している設計上の制約であり、実装の都合で崩してはならない。
変更が必要だと考えた場合は、コードを変えるのではなく、まず仕様書の改訂を提案する。

### 本文の扱い

- 保存本文はファイル先頭の BOM を除外する以外、一切加工しない。
  Unicode 正規化、空白除去、改行の統一を暗黙に行わない。CRLF はそのまま保持する。
- 位置は BOM 除外後の保存本文上の **UTF-16 コード単位** で表し、範囲は `[start, end)` とする。
  フロントエンドとバックエンドで同じ換算を使う。
- 文字数（分割長・文脈長）は **書記素クラスタ数** で数える。位置の単位とは別物として扱う。
- 段落は CRLF・LF・CR のいずれか 1 つで区切られた行。CRLF は 1 つの区切り。
  空行も段落として保持し、区切りの改行列は直前の段落範囲に含める。
- サロゲートペア、異体字セレクタ、結合文字、絵文字の連結、CRLF の途中で分割しない。
- 本文とモデル出力は HTML として実行せず、テキストとして表示する。

### 指摘の位置

- 文字位置はアプリが原文から確定する。LLM が返した数値位置は信用しない。
- 引用は **完全一致** でのみ位置を確定する。近似一致による位置の自動割り当てはしない。
- 完全一致に失敗した候補は「位置特定失敗」として保存し、一覧に表示する。
  診断用の類似候補（改行統一・NFC 比較に限定）は記録するだけで、
  強調表示・再確認・採用位置には使わない。
- 引用の開始位置が検査対象内にある候補だけを採用する。
  参考文脈内から始まる候補は担当範囲へ付け替えず、診断記録に残す。

### 検査処理

- 処理順序は「分割 → 観点別検査 → 引用・位置検証 → 重複統合・許容語後処理 → 再確認 → 結果表示」。
- 観点（誤字・脱字／日本語の自然さ）ごとに別々の要求を送り、要求間で会話履歴を共有しない。
- LLM 生成要求はバックエンド全体で **単一キュー、同時実行数 1**。UI の設定項目にしない。
- 再確認は「維持」「撤回」「作者への確認事項」のいずれかを返し、修正案を改訂しない。
  追加の再確認ループは行わない。
- 再確認の結果は作者の採否を上書きしない。初回判定は履歴に保持する。
- 重複統合は同一の検査実行内に限る。別実行（別モデル・別設定・同条件の再実行）の候補、再確認結果、採否を混ぜない。
- 許容語による自動抑制は「位置確定済みの表記訂正で、引用内の登録語の出現 1 箇所を別の文字列に
  置き換えるだけで修正案を再現できる」場合に限る。登録語内の部分訂正（リュシア→ルシア）は対象、
  登録語の外側に及ぶ変更・削除・複数箇所は対象外。抑制候補は削除せず、非表示にして再閲覧できるようにする。
- 失敗・形式不正を正常な空配列（指摘ゼロ）に置き換えない。
  `finish_reason == "length"` は出力打ち切りであり、JSON が解析できても成功扱いしない。
- 入力上限超過時は本文を黙って切り捨てず、エラーとして設定変更を案内する。
- 本文はデータとして区切り、本文中の命令文に従わない指示をプロンプトに入れる。

### 実行管理

- 「再開」は既存の実行 ID の続き。「新規検査の開始」は原稿版・設定が同じでも常に新しい実行 ID。
  同条件の再実行を既存実行の再開に置き換えない。二重送信の防止は要求の同一性で判定し、条件の一致では判定しない。
- 生成終了を確認できない場合は「復旧待ち」にして自動送信を止め、手動再開を待つ。
  時間経過を終了の証拠とみなさない。
- 「完了」「一部失敗」「停止中」「実行中」を区別する。未処理範囲がある状態を「問題なし」と表示しない。

### 採否と本文

- 「採用予定」「却下」「保留」は評価の記録であり、本文への修正適用ではない。
  MVP では本文を書き換える機能を一切作らない。

### セキュリティ・プライバシー

- 原稿と検査結果はローカルに保存し、外部サービスへ送信しない。
- Web アプリは初期設定でループバックアドレスのみで待ち受ける。
- API キーを検査履歴・ログ・結果出力に含めない。

## 対象外（作らない）

仕様書 3.2 節のとおり。特に以下は「あると便利」に見えても MVP では実装しない。

- 本文への修正適用、手動編集、diff 表示、取り消し・やり直し
- 近似一致による位置の自動割り当て
- 設定・時系列・人物などの整合性検査、推敲提案
- RAG、複数モデル、ファインチューニング
- Word・PDF 読み込み、ルビ記法の専用解析
- クラウド公開、複数ユーザー

## 未決事項の扱い

仕様書 13 節の未決事項（技術選定、接続先、プロンプト、タイムアウト、分割長の実測値など）は
エージェントが独断で確定しない。選択肢と推奨を示し、ユーザーの決定を待つ。
未決であることを機能範囲を広げる理由にしない。

## 開発規約

### コミット・ブランチ

- `main` に直接コミットせず、作業ごとにブランチを切る（初期セットアップのみ例外）。
- コミットメッセージは日本語。1 行目に変更の要約、必要なら空行を挟んで理由を書く。
- 仕様書を改訂したら、`mvp-spec.md` の改訂記録（15 節）に追記し、同じコミットに含める。

### テスト

- 仕様書 11 節の受け入れ条件を、可能な限り自動テストに落とす。
- 以下は境界条件テストを必須とする。
  - 段落分割: CRLF・LF・CR の混在、末尾改行の有無、空行、単一の長い段落
  - 書記素クラスタ境界: サロゲートペア、異体字セレクタ、結合文字、絵文字の ZWJ 連結
  - 位置換算: UTF-16 コード単位と書記素クラスタ数のずれがある本文
  - 引用照合: 同一引用の反復、検査対象と参考文脈の境界をまたぐ引用、本文端
  - LLM 応答: 不正 JSON、`<think>` を含む出力、`finish_reason == "length"`、空配列
- LLM を実際に呼ぶテストはローカル環境依存なので、通常のテストからは分離する。

### コードの方針

- 単位ごとに責務を 1 つに絞り、テストしやすい境界を作る。
  特に「分割」「照合」「統合・抑制」「LM Studio クライアント」「永続化」は独立させる。
- 実装時に仕様の解釈に迷ったら、推測で進めずコメントかドキュメントで疑問点を残す。

## 技術スタック（選定後に追記）

未決。選定後に以下を追記する。

- 使用言語・フレームワーク・保存形式
- セットアップ、起動、テスト、lint の各コマンド
- ディレクトリ構成

## 参照

- `mvp-spec.md` — MVP 仕様書（正本）
- [LM Studio: OpenAI 互換 API](https://lmstudio.ai/docs/developer/openai-compat)
- [LM Studio: 構造化出力](https://lmstudio.ai/docs/developer/openai-compat/structured-output)
