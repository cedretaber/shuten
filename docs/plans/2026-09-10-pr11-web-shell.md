# PR11 詳細計画：接続・入力・設定画面（web）

作成日：2026-09-10
状態：計画済み（タスク分解まで）
仕様：`docs/spec/mvp-spec.md`（v0.9）4（手順 1〜4）、5.1、5.2、7（モデル種別による絞り込み）、
8.2（二重送信、再開と新規の区別）、9（ループバック）、11 節（1・2 項の UI 側）
前提：`docs/plans/2026-09-07-mvp-roadmap.md` の PR11 節、`docs/reference/invariants.md`、
`docs/plans/2026-09-09-pr10-http-api.md`（決定 1〜19。API の契約はここが正本）

## 目標

PR10 で用意した HTTP API を、ブラウザから使える画面にする。すなわち、

- アプリの骨格（ルーティング、共通レイアウト、ヘッダーの接続状態）
- API クライアント（`fetch` の薄い包み。型と zod スキーマは `packages/shared/src/api/` から取る）
- `features/connection/`（接続先・API キー・接続確認・モデル選択）
- `features/manuscript/`（貼り付けとファイル読み込み、原稿版の確定）
- `features/settings/`（検査設定と検査の開始）
- `features/run-receipt/`（開始した実行の受付表示）
- コンポーネントの単体テスト基盤（Vitest ＋ Testing Library。導入はこの PR）

本 PR の終点は「検査を開始でき、実行 ID と状態が表示される」ところまでである。
結果の閲覧は PR12a、進捗・実行制御・SSE は PR12b が作る。

## Windows での確認（着手の前提ではなく、マージ条件）

ロードマップのチェックポイント「Windows でのローカル確認」は、当初 PR11 の**前**に置いていたが、
2026-09-10 に **PR11 の実装後・マージ前**へ移した。理由は 2 つある。

- CI の `check` ジョブは `matrix: [ubuntu-latest, windows-latest]` で回っており、Windows 側で
  依存の導入・型検査・lint・テスト・`pnpm build`・better-sqlite3 のバインディング読み込みまで通っている。
  ガイドの旧手順 1・2・3・5 は CI が毎回確認している内容と同じである。
- CI が確認していないのは `pnpm start` から先（静的配信、SPA の直リンク、ブラウザ操作、LM Studio 接続、
  コンソールからの終了）だけで、**それはまさに本 PR が作る画面である**。PR11 より前に確認しても、
  確認対象が本 PR で置き換わる。

したがって手順は次の順で行う。

1. PR11 を実装する
2. CI を Ubuntu・Windows とも緑にする
3. PR11 のブランチをネイティブ Windows で起動確認する（`docs/guides/windows-verification.md` の 2 節）
4. 問題があれば同じ PR で直す
5. マージする

3 に **Ctrl+C による終了の確認を含める**。`packages/server/src/index.ts` の graceful shutdown は
「Windows には `SIGTERM` が届かず、コンソールの Ctrl+C（`SIGINT`）だけが対象」という前提で書いてあるが、
その経路は CI を通らず、PR10 の時点でも実機確認をしていない。`shutdown: done` まで進んでプロセスが
終了し、もう一度 `pnpm start` できること（SQLite のハンドルが閉じていること）まで見る。

結果は `docs/decisions/0002-scaffold-conventions.md` の「検証状況」に記録する。合成原稿の本文や
機器固有の値（IP アドレス、ユーザー名を含むパス）は記録に含めない。

## 対象外（MUST NOT）

- **結果の閲覧を作らない**（PR12a）。本文の表示と強調、指摘一覧、指摘詳細、採否、絞り込み。
- **進捗と実行制御を作らない**（PR12b）。SSE、ポーリング、停止・再開・再試行、復旧確認の操作。
- **実行一覧を作らない**（PR12a）。`GET /api/runs` を呼ばない。
- **原稿の削除・一覧・自動掃除を作らない。** 検査に使われないまま残る原稿版は許容する（決定 11）。
- **本文を編集する経路を作らない。** 確定後の本文は読み取り専用の表示だけ。
- **エクスポートを作らない**（PR13a）。
- **サーバーの API を増やさない。** 本 PR で `packages/server` に触るのは
  `isGenerationCapable` を `packages/shared` へ移す 1 件だけ（決定 9）。
- **API キーを `localStorage` に置かない。接続先 URL を接続設定画面以外に出さない**（決定 18）。
- **CSS フレームワーク・状態管理ライブラリ・API モックライブラリを入れない**（決定 4・5）。
- `react-router` の `loader` / `action` / `createBrowserRouter`（データルーター）を使わない（決定 3）。

## 全体の制約（不変条件から）

- 本文は BOM 除外以外を加工しない。ファイル入力は CRLF を含む元の改行を保持する。
  **ブラウザは本文を一切加工しない**（決定 11）。
- 分割範囲と確定位置の正本はサーバーが保存した UTF-16 範囲。ブラウザは再計算結果で上書きしない。
- 文字数は書記素クラスタ数で数える。書記素クラスタの途中で文字列を切らない（決定 17）。
- 未ロードのモデルに生成要求を送らない。検査開始の直前にロード状態を確認する（決定 8・9）。
- API キーを検査履歴・ログ・結果出力に含めない。接続先 URL を結果表示・エラー表示に出さない。
- 依存の版は完全固定。相対 import は `.ts` 拡張子付き。scripts は Windows で動く書き方。
- 公開リポジトリ。個人情報・機器固有の値・実原稿をコミットしない。
- ドキュメント・コミットメッセージ・コード内コメントは日本語、識別子は英語。

## 作るもの

| ファイル | 内容 |
| --- | --- |
| `packages/web/src/main.tsx` | エントリ。`<BrowserRouter>` で `<App />` を包むだけ |
| `packages/web/src/App.tsx` | `<Routes>` の定義。scaffold の書記素計数の画面を置き換える |
| `packages/web/src/app/routes.ts` | パス定数と `runPath(id)`。パス文字列をここ以外に書かない |
| `packages/web/src/app/layout.tsx` | ヘッダー＋本文の共通レイアウト |
| `packages/web/src/app/header.tsx` | 接続状態・選択モデル・最終確認時刻・再確認・接続設定へのリンク |
| `packages/web/src/app/connection-context.tsx` | 接続状態と選択モデルの共有、世代番号と `AbortController`（決定 10） |
| `packages/web/src/app/not-found.tsx` | 画面内 404 |
| `packages/web/src/app/home-page.tsx` | `/` の画面。原稿と検査設定の feature を組み合わせる |
| `packages/web/src/app/model-selection.ts` | `canStartWithModel` / `nextSelectedModelId`（決定 6・9） |
| `packages/web/src/api/client.ts` | `createApiClient({ fetch })`。PR11 が使う 8 つの口だけ（決定 14） |
| `packages/web/src/api/errors.ts` | `ApiRequestError` / `ApiResponseError` / `ApiTransportError`、`isStartOutcomeUnknown`（決定 15） |
| `packages/web/src/api/context.tsx` | `ApiClientProvider` / `useApiClient`。`createApiClient()` は `App.tsx` で 1 回だけ呼ぶ |
| `packages/web/src/storage/keys.ts` | `localStorage` のキー定数（決定 6・7） |
| `packages/web/src/storage/local.ts` | 読み書きの薄い包み。zod で検証し、壊れていたら捨てる |
| `packages/web/src/features/connection/` | `/settings/connection` の画面 |
| `packages/web/src/features/manuscript/` | 原稿の 2 状態（編集中・確定済み）（決定 11） |
| `packages/web/src/features/settings/` | 検査設定フォームと開始操作（決定 12・13） |
| `packages/web/src/features/run-receipt/` | `/runs/:id` の受付表示（決定 16） |
| `packages/web/src/styles/tokens.css` | 色・余白・文字サイズのカスタムプロパティ |
| `packages/web/src/styles/global.css` | リセットと共通レイアウト |
| `packages/web/vitest.setup.ts` | `@testing-library/jest-dom/vitest` の登録と `afterEach(cleanup)`（決定 19） |
| `packages/shared/src/run/model-capability.ts` | `isGenerationCapable`（server から移す。決定 9） |

`features/*` の中は画面の `*.tsx`、その feature 固有の `*.module.css`、`*.test.tsx` で構成する。
共通コンポーネント（ボタン、入力欄、エラー表示）は**最初は作らない**。3 つの feature を書き終えて
重複が実際に見えてから切り出す。

---

## 決定

### 決定 1：本 PR の終点は「実行の受付表示」まで

検査の開始（`POST /api/runs`）が実際に通るところまでを本 PR に含める。仕様 4 節の手順 4 が
「検査を開始する」であり、ロードマップが「11 節 1 項は最初の検査実行で満たす」としているため。

開始した直後に利用者が見るのは、次だけとする。

- 再表示可能な URL（`/runs/:id`）へ遷移する。
- 表示するのは実行 ID と現在の `status`（および `stopReason` / `stopMessage`）。
- **初回・再読み込み・直リンクのすべてで `GET /api/runs/:id` を 1 回だけ呼ぶ。**
  `POST /api/runs` の応答は遷移先の ID を得るためだけに使い、画面へ渡さない。手動の
  「最新の状態を取得」だけ用意する。ローカル通信が 1 回増えるが、履歴 `state` の残存や
  古いスナップショットの表示を避けられる。
- 進捗、結果、停止・再開・再試行、SSE、ポーリングは**含めない**。

存在しない実行 ID、取得失敗、開始失敗の表示を用意する（決定 16）。

### 決定 2：画面割りは「主動線 1 ページ ＋ 接続は別画面」

| パス | 画面 | 中身 |
| --- | --- | --- |
| `/` | 原稿と検査設定 | 原稿の確定 → 検査設定 → 開始 |
| `/settings/connection` | 接続設定 | 接続先 URL、API キー、接続確認、モデル選択 |
| `/runs/:id` | 受付表示 | 実行 ID と状態（決定 1） |
| `*` | 画面内 404 | 「そのページはありません」と `/` へのリンク |

接続設定はサーバーの `settings` 表に残るため、実際に触るのは初回と接続先を変えるときだけである。
毎回の動線から外し、ヘッダーに状態を常時表示する。ヘッダーは PR12a で結果画面が増えても使える。

### 決定 3：ルーティングは `react-router` の宣言的 API だけを使う

使うのは `BrowserRouter` / `Routes` / `Route` / `Link` / `useParams` / `useNavigate` / `useLocation`、
テストでは `MemoryRouter`。`loader` / `action` / `createBrowserRouter` は使わない。通信は API
クライアントに置き、ルーターにデータ取得の責務を持たせない（PR12 で責務が混ざるのを防ぐ）。

自前の薄いルーターを書く案は退けた。100 行程度で書けても、`popstate`、`pushState` 後の再描画、
修飾キー付きクリック（Ctrl/Cmd ＋クリックで別タブ）、パスパラメーターの解析、直接アクセス、
テスト用の履歴を自分で正しく扱う必要がある。PR12a で実行一覧、PR12b で状態復元が加わり、
ルーティングは一時的な補助ではなくアプリの骨格になる。保守対象を増やす費用のほうが大きい。

`react-router@8.3.1` は `BrowserRouter` などを本体エントリから公開しており、`react-router-dom` は
要らない。peer は `react >=19.2.7`（当リポジトリは 19.2.8）、直接依存は `cookie-es` だけである。

サーバー側の SPA フォールバックは PR10 で入っている（`packages/server/src/app.ts`。`/api` 以外は
`index.html` を返す）。vite の dev server も既定でフォールバックする。History API 方式に
server 側の変更は要らない。

### 決定 4：足す依存はこれだけ

```
dependencies:
  react-router                    8.3.1
devDependencies:
  @testing-library/dom            10.4.1   （@testing-library/react の peer）
  @testing-library/jest-dom       7.0.1
  @testing-library/react          16.3.3
  @testing-library/user-event     14.6.7
  jsdom                           30.0.1
```

`@testing-library/dom` は `@testing-library/react@16.3.3` が peer として要求する。**要求される peer の
版を明示的に管理するため**、直接 devDependency に固定する（自動解決の挙動は pnpm の設定と版に依存する
ので、そこに任せない）。

足さないもの：CSS フレームワーク、UI コンポーネントライブラリ、TanStack Query などのサーバー状態
管理、MSW。PR11 の通信は接続確認・原稿作成・原稿取得・開始・実行 1 件の取得だけで、キャッシュも
自動再検証も要らない。SSE と進捗が入る PR12b で改めて検討する。API のモックは `fetch` の注入で行う
（決定 14）。

### 決定 5：CSS は素の CSS ＋ CSS Modules

- 色・余白・文字サイズを CSS カスタムプロパティで `styles/tokens.css` に定義する。
- リセットと共通レイアウトを `styles/global.css` に置く。
- feature 固有のスタイルは CSS Modules（vite 標準。依存ゼロ）。
- ボタン・入力・エラー表示の共通コンポーネント化は、反復が確認できてから行う。

PR12a の本文表示・指摘一覧・強調表示・左右ペインは独自性が高く、既成のフレームワークを入れても
中心部分は個別実装になる。設定・更新・命名規則・アクセシビリティ確認の対象を増やす費用に見合わない。

### 決定 6：ブラウザ側の永続化は `localStorage`

| キー | 値 | 既定 |
| --- | --- | --- |
| `shuten.v1.selectedModelId` | モデル ID の文字列 | 未選択 |
| `shuten.v1.runSettings` | 検査設定一式（許容語を除く） | `RUN_SETTINGS_DEFAULTS` |
| `shuten.v1.allowedWords` | `{ manuscriptVersionId, allowedWordsRaw }`（決定 7） | なし |
| `shuten.v1.manuscriptVersionId` | 直近に確定した原稿版 ID | なし |

**置かないもの**：API キー（サーバーのプロセスメモリだけ、という PR10 決定 5 を崩さない）、
接続先 URL（サーバーの `settings` 表が正本）、本文（正本はサーバーの原稿版。10 万字級では容量的にも
入らない）。

読み出しは必ず zod で検証し、壊れていたらそのキーを消して既定値に戻す。`localStorage` が使えない
環境（プライベートウィンドウなど）で例外が出ても画面が落ちないよう、読み書きを try/catch で包む。

保存する検査設定は `StartRunRequest` とは**別の形**である（`startOperationId`・`manuscriptVersionId`・
`modelId`・`allowedWordsRaw` を含まない）。そのため `storage/local.ts` に専用の
`storedRunSettingsSchema` を定義する。`startRunRequestSchema` を流用しない。

起動時に `shuten.v1.selectedModelId` を接続確認へ渡し、次の規則で扱う（決定 9 の「未ロードでも選択は
維持する」と整合させる）。

| 接続確認の結果 | 保存済みの選択 |
| --- | --- |
| 接続に失敗した | **維持する**（モデルの情報が得られていないため） |
| 一覧は取れたが、その ID が無い | 解除する |
| ID はあるが種別が `llm` / `vlm` でない | 解除する |
| `llm` / `vlm` だが未ロード | **維持する**。ロードを案内する |
| ロード済みの `llm` / `vlm` | 維持する。開始できる |
`shuten.v1.manuscriptVersionId` は `GET /api/manuscripts/:id` で確かめ、**404 ならキーを消して
編集中に戻す**。通信失敗・5xx では**消さず**、再試行可能なエラーとして扱う。

### 決定 7：許容語は原稿版と組にして保存する

許容語は作品固有の固有名詞・造語であることが多い。別の原稿へ自動的に持ち越すと、本来出すべき指摘を
黙って抑制することになる。仕様 6.4 の「語の登録だけで周囲の文法や文脈上の誤用まで免除しない」の
趣旨にも反する。

`{ manuscriptVersionId, allowedWordsRaw }` の組で保存し、**同じ原稿版を再表示したときだけ復元する**。
別の原稿を確定したら空から始める。分割長・観点・再確認の有無・生成設定は作品に依存しないので、
従来どおり `shuten.v1.runSettings` に共通で持つ。

### 決定 8：接続確認を投げる契機は 3 つだけ

1. アプリの起動時に 1 回（保存されたモデル ID を添える）
2. 接続設定を保存した直後
3. **検査を開始する直前**

**定期的なポーリングはしない。** 3 が要るのは、`POST /api/runs` の 201 が「作成直後のスナップショット
で、モデル未ロードなどの結末は含まない」ためである（PR10 決定 14）。PR11 には進捗表示が無いので、
未ロードのまま開始すると利用者には `running` に見えたまま黙って失敗する。

ヘッダーの表示は「**最後に確認した時点の状態**」であることが分かる表現にする（確認時刻を添え、
手動の「再確認」を置く）。ポーリングしない以上、LM Studio 側のロード状態は表示後に変わりうる。

`POST /api/settings/connection/check` は試験生成を送らないので（PR10 決定 8）、副作用は無い。

### 決定 9：開始できるモデルの条件は `isGenerationCapable`

開始してよいのは、次がすべて成り立つときだけである。

```ts
const selected = check.models.find((model) => model.id === selectedModelId);

const canStart =
  check.reachable &&
  check.model?.found === true &&
  selected !== undefined &&
  isGenerationCapable(selected);
```

`find()` は `undefined` を返しうるので、`isGenerationCapable` に渡す前に必ずガードする。

`loaded` だけを見てはならない。`loaded` はロード状態しか表さず、embeddings モデルでもロード済みなら
`true` になる。サーバーの生成経路（`packages/server/src/run/executor.ts`）は
`state === "loaded" && (type === "llm" || type === "vlm")` を要求する（仕様 7 節）。ここを揃えないと、
画面上は開始できたのに実行が黙って止まる。

**`ConnectionCheckDto.model` は `{ id, found, state, loaded }` だけで `type` を持たない。**
`type` があるのは `check.models[]`（`ModelInfoDto`）のほうである。判定は `check.models` から
当該モデルを引いて行う。

判定規則を web に書き写さず、`isGenerationCapable` を
`packages/server/src/lmstudio/models.ts` から `packages/shared/src/run/model-capability.ts` へ移し、
server 側は薄い委譲に変える。shared 側は `{ state: string | null; type: string | null }` を受ける
構造的な引数にする。server の `ModelInfo`（`lmstudio/types.ts`）も shared の `ModelInfoDto`
（zod の `z.string().nullable()`）も `type` / `state` が `string | null` なので、どちらもそのまま渡せる。
PR10 で `RUN_STOP_REASONS` などの列挙を shared に移したのと同じ扱いである。二重に持つと片方だけ
直す事故が起きる。

判定に使う `LOADED_STATE`（`"loaded"`）は現在 `packages/server/src/lmstudio/types.ts` にあり、
`client.ts` と `api/settings.ts` も参照している。これも同じファイルへ移し、
`lmstudio/types.ts` からは shared のものを**再エクスポート**して既存の import を壊さない。

モデル一覧の選択肢には `llm` / `vlm` だけを出す。未ロードのものも選択状態は維持し、
「LM Studio でロードしてください」と表示したうえで開始はさせない。種別が `null` や `embeddings` の
ものは選択肢に出さない。

### 決定 10：接続確認の競合は世代番号と `AbortController` で防ぐ

起動時・保存直後・開始直前の確認は重なりうる。古い要求が後から完了して新しい状態を上書きしないよう、
`app/connection-context.tsx` に世代番号を持ち、**最新の要求の応答だけを反映する**。あわせて新しい
要求を出すときは前の要求を `AbortController` で中断し、アンマウント時も中断する。

`signal.aborted` による中断は、画面上の通信エラーとして**表示しない**。

開始処理は context の `checkConnection()` を呼び、**その戻り値を直接判定に使う**。context の更新後の
React state を読み直す形にしない（再描画の順序に依存する）。API クライアントを直接呼ぶのも不可で、
世代番号・`AbortController`・ヘッダーの最終確認時刻の更新を迂回してしまう。

```ts
const result = await connection.checkConnection(modelId);
if (!canStartWithModel(result, modelId)) return;
await api.startRun(snapshot);
```

### 決定 11：原稿版は明示の確定操作で作る

画面の状態は 3 段階とする。

1. **編集中** — 貼り付けた本文、または選択した `File` を保持する。**サーバーには送らない。**
2. **原稿確定済み** — `POST /api/manuscripts`（貼り付け）または `POST /api/manuscripts/upload`
   （ファイル）を実行し、返された原稿版 ID を保持する。サーバーが保存した本文（＝正本）を
   読み取り専用で表示する。
3. **検査受付済み** — 確定済みの原稿版 ID で `POST /api/runs`。開始に失敗しても原稿版 ID を保持し、
   原稿を作り直さず**開始だけを再試行する**。

利点は「孤立した原稿版を発生させない」ことではない。確定後に「別の原稿を選ぶ」を押したり、検査せず
画面を閉じたりすれば、実行から参照されない原稿版は残る。正確には
**「原稿版の作成を利用者の明示的な確定操作にし、検査開始に失敗しても同じ原稿版を再利用する。
開始の再試行によって原稿版が増えることを防ぐ」**である。MVP では削除・原稿版一覧・自動掃除を
追加せず、この未使用レコードを許容する（ローカル利用で数も限られ、清掃機能はライフサイクル設計を
大きくする）。

ファイル入力では、選択した `File` を確定操作まで保持し、**`File.text()` も textarea の値も保存要求に
使わない**。確定時に元の `File` をそのまま multipart で送る。仕様 5.1 の「編集欄の改行変換を保存本文へ
逆流させない」を守るにはこれしかない。CRLF と不正 UTF-8 の判定はサーバーへ一元化される
（不正なら 400 `invalid-utf8`）。

ボタン名は役割で分ける。

- 第 1 段階：「この原稿を確定する」
- 第 2 段階：「検査を開始する」
- 確定後：「別の原稿を選ぶ」（**現在の選択を解除するだけ**。新しい本文を再度確定した時点で次の
  原稿版を作る）

確定後の表示には原稿名・書記素数・確定日時を出し、「**確定後の本文は変更できない**」と明示する。

「この原稿を確定する」の無効化条件は次の 3 つ。

- 貼り付けで本文が空（`length === 0`。**`trim()` はしない** — 空白だけの本文も無加工で保存する対象）
- ファイル未選択
- 原稿名が空、または 201 文字以上

検査設定は**原稿を確定するまで操作できない**。

### 決定 12：検査設定は仕様 5.2 の 6 項目を既定表示、残りは折りたたむ

既定で見せるのは仕様 5.2 の表の 6 つ（検査観点、分割長、初回の参考文脈、再確認の有無、
再確認の参考文脈、許容語）。残り（`maxTokens`、`temperature`、`seed`、`reasoningEffort`、
`roundingTolerance`、`maxInputGraphemes`、`checkMs`、`recheckMs`）は折りたたみの
**「詳細設定」**に入れる（分割の許容幅・入力上限・タイムアウトを含むので「生成設定」ではない）。
**変えられるが、既定では見せない。**

初期値は `RUN_SETTINGS_DEFAULTS`。思考は既定で `none`。`seed` は空欄なら送らない。既定値から
変えた項目があれば、閉じたままでもそれと分かるようにする。

画面での単位と API の単位が違う項目は、次に固定する。往復の変換は 1 か所にまとめる。

| 項目 | 画面での入力 | 送信する値 |
| --- | --- | --- |
| `checkMs` / `recheckMs` | **秒**（整数 1〜2,147,483。上限は `Math.floor(MAX_TIMEOUT_MS / 1000)`） | 入力 × 1000 |
| `roundingTolerance` | **パーセント**（整数 0〜99。仕様 5.2 が「±20%」と書いているため） | 入力 ÷ 100 |
| `maxInputGraphemes` | 字（書記素数）。変換なし | そのまま |
| `maxTokens` / `temperature` / `seed` | そのまま | そのまま |

パーセントの整数 0〜99 は `validateChunkSettings` の「0 以上 1 未満」にそのまま対応する。
往復については、JavaScript の `Number` で `0.2 * 100 === 20` と `20 / 100 === 0.2` が成立することを
確認した（0.15 も同様）。0.2 が二進数で厳密に表現できるという意味ではない。

根拠は仕様 5.2 末尾の「温度などの最適値や推論モードは現時点で固定しない」と、13 節が
「モデルごとのプロンプト、生成パラメーター、タイムアウト」「分割長と参考文脈長の実測による調整」を
未決にしていること。PR13b の評価で実際に動かす値なので、画面から触れないと毎回 CLI か curl に戻る。

### 決定 13：入力検証の分担

**クライアントで弾く**（shared が持っている規則だけ）：

- 組み立てた `StartRunRequest` 全体を `startRunRequestSchema.safeParse` に通す。整数性、
  各タイムアウトの `1..MAX_TIMEOUT_MS`、観点 1 つ以上がまとめて確認できる。
  **`StartRunRequest` に原稿名は含まれない。** 原稿名の 1〜200 文字は原稿の確定時に
  `createManuscriptRequestSchema.shape.name` で別途検証する。
- `validateChunkSettings`（分割設定の整合）。
- 観点は最低 1 つ（最後のチェックを外せなくする）。
- 原稿名 1〜200 文字、本文が空でない。

**サーバーに委ねる**：

- `checkMs + recoveryConfirmMs` の合計上限（`validateHardTimeouts` は `recoveryConfirmMs` を要り、
  これはサーバー設定である）。
- 本文から判明する入力上限超過（決定 16 で受付画面が扱う）。

サーバーから返ったエラーは項目ごとではなく**フォーム全体のエラー**として、開始ボタンの上に 1 か所に
出す。

### 決定 14：API クライアントは `fetch` を注入できる薄い包み

`api/client.ts` に `createApiClient({ fetch })` を置く。既定は `globalThis.fetch`。注入できるように
するのはテストで fake を渡すためで、これが MSW を入れない理由でもある（server 側のテストが fake を
渡す流儀と揃う）。

**PR11 が呼ぶ口だけを作る。**

| 関数 | 呼ぶ口 |
| --- | --- |
| `getConnection()` | `GET /api/settings/connection` |
| `putConnection(body)` | `PUT /api/settings/connection` |
| `checkConnection(modelId?, { signal })` | `POST /api/settings/connection/check` |
| `createManuscript({ name, body })` | `POST /api/manuscripts` |
| `uploadManuscript({ file, name })` | `POST /api/manuscripts/upload` |
| `getManuscript(id, { signal })` | `GET /api/manuscripts/:id` |
| `startRun(body)` | `POST /api/runs` |
| `getRun(id, { signal })` | `GET /api/runs/:id` |

`GET /api/runs`（一覧）、停止・再開・再試行、指摘、採否、復旧確認、SSE は PR12 の担当なので
**書かない**。

応答はすべて `@shuten/shared` の zod スキーマで `parse` する。サーバー側も `respond` で出力を検証して
いるので二重だが、型注釈と実際の JSON がずれたときに実行時に気づける。スキーマは `.strict()` なので、
**余分なキーを含む応答は拒否される**（落とされるのではない）。

例外は 3 種類。

| 例外 | いつ |
| --- | --- |
| `ApiRequestError { status, code, message }` | 非 2xx。本文を `apiErrorSchema` で parse して得る |
| `ApiResponseError` | 2xx だが本文が JSON でない、またはスキーマに合わない |
| `ApiTransportError` | `fetch` 自体が throw した、または**本文の読み取り中に切れた** |

非 2xx の本文が `apiErrorSchema` に合わないとき（HTML が返ってきた等）は、状態コードだけを持つ
`ApiRequestError` に落とし、`code` は `"unknown"` にする。

通信途中の切断と不正な JSON を確実に区別するため、`response.json()` は使わず、**本文の読み取り・
JSON の解析・状態コードによる分岐を、この順で分ける**。解析の失敗を即座に例外にしてはならない。
非 2xx の HTML 本文は `ApiResponseError` ではなく `code: "unknown"` の `ApiRequestError` である。

```ts
const text = await readResponseText(response); // 読み取りに失敗したら ApiTransportError

let json: unknown;
let parsedAsJson = true;
try {
  json = JSON.parse(text);
} catch {
  parsedAsJson = false; // ここでは投げない
}

if (!response.ok) {
  const parsedError = parsedAsJson ? apiErrorSchema.safeParse(json) : null;
  if (parsedError?.success) {
    throw new ApiRequestError(
      response.status,
      parsedError.data.error.code,
      parsedError.data.error.message,
    );
  }
  // 非 2xx で本文が JSON でない、または apiErrorSchema に合わない
  throw new ApiRequestError(response.status, "unknown", GENERIC_REQUEST_ERROR);
}

if (!parsedAsJson) {
  throw new ApiResponseError(); // 2xx なのに JSON でない
}

return responseSchema.parse(json); // zod の失敗も ApiResponseError に写す
```

結果として、分類はこうなる。

| 状況 | 例外 | `isStartOutcomeUnknown` |
| --- | --- | --- |
| 本文の読み取りに失敗 | `ApiTransportError` | true |
| 4xx で本文が HTML／不正 JSON | `ApiRequestError(status, "unknown")` | **false**（確定） |
| 5xx で本文が HTML／不正 JSON | `ApiRequestError(status, "unknown")` | **true** |
| 2xx だが JSON でない、またはスキーマ違反 | `ApiResponseError` | true |

multipart は `FormData` に `file`（**選択した `File` をそのまま**）と `name` を入れ、`Content-Type` は
**指定しない**（ブラウザに boundary を付けさせる。自分で書くと壊れる）。

クライアント独自のタイムアウトは**設けない**。PR11 が呼ぶのは同期処理だけで（`POST /api/runs` は
分割計画までで、生成は投げない）、独自の打ち切りは「結果不明」の状態を自分で増やすことになる。

### 決定 15：開始結果が不明かどうかは例外クラスと別の述語で判定する

`startOperationId` の使い回しは、例外の種類ではなく `isStartOutcomeUnknown()` という別の述語で決める。

| 結果 | 条件 | `startOperationId` |
| --- | --- | --- |
| **確定** | 有効な `RunDto` を受け取った | 使い切り。押し直しは新しい ID |
| **確定** | 4xx で**本文を最後まで読めた**（`apiErrorSchema` に合うかどうかは問わない） | 使い切り。押し直しは新しい ID |
| **不明** | `fetch` が throw した | 保持して再試行 |
| **不明** | 本文の読み取り中に切れた | 保持して再試行 |
| **不明** | 2xx だが契約違反（JSON でない、スキーマに合わない） | 保持して再試行 |
| **不明** | 5xx | 保持して再試行 |

5xx を「不明」に入れるのが要点である。サーバーが実行を作った後の処理で落ちた可能性があるため、
確定扱いにすると二重に作る。

`POST /api/runs` は**二重送信を 409 にしない**。同じ `startOperationId` の既存実行を見つけたら、
残りの入力を検証も比較もせずに **200** でその実行を返す（PR9b 決定 12、PR10 決定 14。
`api/runs.ts` の手順 2）。新規作成は 201 である。したがって同じ ID での再送は安全で、画面は
**200 でも 201 でも同じように `RunDto` を受け取って `/runs/:id` へ遷移する**。再試行のテストは
「200 を受け取って既存の実行へ遷移する」ことを見る（201 を期待しない）。

4xx は本文を最後まで読めた時点で確定とする。`apiErrorSchema` に合わない本文（HTML が返ってきた等）は
`code: "unknown"` の `ApiRequestError` になるが、**サーバーが要求を受け取って断ったこと自体は
確定している**ので、結果不明にはしない。結果不明になるのは本文の読み取り自体が失敗した場合だけである。

作成前の拒否は 400（`validation` / `invalid-run-settings`）と 404（原稿版なし）で、開始経路に
409 は無い（409 を返すのは `PUT /api/settings/connection` の `runs-active` と、停止・再開・再試行の
経路であり、いずれも本 PR の開始処理とは関係しない）。

**再送は ID だけでなく要求内容も固定する。** 送信開始時に `StartRunRequest` 一式をメモリに
スナップショットとして保持し、「不明」からの再試行はそれをそのまま送る。ID だけ固定してフォームの
現在値を送ると、サーバーは既存実行を返すのに画面は新しい設定を映していて、食い違ったまま気づけない。

送信中はボタンを `disabled` にする（連打の防止）。

### 決定 16：受付画面は常に中立表示、状態は網羅対応表で日本語に写す

`GET /api/runs/:id` の `RunDetailDto` を 1 回だけ取る。**ポーリングも SSE もしない。**
手動の「最新の状態を取得」だけ用意する。

画面が読むのは **`RunDto` だけ**である。`POST /api/runs` は `RunDto` を直接返し、
`GET /api/runs/:id` は `{ run: RunDto, progress, targets }` を返すので、どちらの経路でも
`RunDto` を同じ描画関数に渡せる。`progress` と `targets` は**使わない**（進捗の表示は PR12b）。

見出しは**常に中立の「検査実行」**とし、開始直後かどうかで文言を変えない。`location.state` は
履歴の扱いによって再読み込み後も残りうるので、「開始直後だけ」の判定に使わない。

`RunStatus`（5 種）と `RunStopReason`（8 種）は、**`satisfies Record<RunStatus, string>` /
`satisfies Record<RunStopReason, string>` を付けた対応表**で日本語に写す。`default` の無い `switch`
は書き方によっては列挙が増えても型検査が落ちないので、対応表のほうを使う。

| `RunStatus` | 表示 |
| --- | --- |
| `running` | 実行中 |
| `stopped` | 停止中 |
| `recovery-waiting` | 復旧待ち |
| `completed` | 完了 |
| `partially-failed` | 一部失敗 |

`RunStopReason` は `model-not-loaded` / `recovery-needed` / `connection-lost` / `settings` /
`aborted` / `internal-error` / `recovery-blocked` / `backend-restarted` の 8 つすべてを写す。

`stopMessage` はそのまま表示する。PR10 の制約により接続先 URL を含まないことが保証されている
（`orchestrator.start.test.ts` の O16 が検証している）。

**`stopped` かつ `stopReason === "settings"` は受付成功として見せない。** 「検査は開始できませんでした」
に切り替え、`stopMessage`（入力上限の説明）と「検査設定に戻る」を出す。`POST /api/runs` は 201 を
返すが、その中身がすでに `status: "stopped"` / `stopReason: "settings"` / `stopMessage` 非 null に
なっている場合があるためである（分割時の `InputTooLongError`。`orchestrator.start.test.ts` の
O16・I-1）。仕様 5.2 の「入力上限を超えた場合は黙って縮めず、エラーとして設定変更を案内する」は
ここで満たす。再読み込み後の `GET` でも同じ判定なので表示は変わらない。

404 は「その実行はありません」と `/` へのリンク。

### 決定 17：本文のプレビューは書記素境界で切り、全文は開いたときだけ描画する

確定済みの原稿は、既定で**先頭 500 書記素**のプレビューを出す。`body.slice(0, 500)` は使わない
（サロゲートペア、結合文字、ZWJ 絵文字、CRLF の途中で切れる）。`buildGraphemeIndex(body)` と
`offsetAt` で UTF-16 位置を求めて切る。両方 `@shuten/shared` から公開されている。

**`offsetAt(index, 500)` を直に呼んではならない。** `offsetAt` は範囲外（`grapheme > index.count`）で
`RangeError` を投げるので、本文が 500 書記素未満だと必ず落ちる。

```ts
const index = buildGraphemeIndex(body);
const previewCount = Math.min(index.count, PREVIEW_GRAPHEMES); // PREVIEW_GRAPHEMES = 500
const preview = body.slice(0, offsetAt(index, previewCount));
```

本文が 500 書記素以下なら、プレビューが全文と一致するので**全文展開の操作自体を出さない**。

全文は折りたたみの中に置くが、**開閉状態を React で持ち、開いているときだけ全文を DOM に足す**。
閉じた `<details>` の子要素も DOM には存在するので、`<details>` に入れるだけでは 10 万字級の描画を
避けられない。全文は `white-space: pre-wrap` で表示する。

書記素数は確定後に `countGraphemes(body)` で **1 回だけ**数える（入力中に毎打鍵で数えない）。

### 決定 18：API キーの扱い

- API キーは `input[type="password"]` の書き込み専用欄で受け取る。**その入力値以外のテキストとして
  描画しない。**
- エラー表示、接続済み表示、ヘッダーに含めない。
- `PUT /api/settings/connection` の要求本文以外の要求へ送らない。
- 例外オブジェクトへ要求本文や API キーを格納しない。
- 保存成功後に入力欄を空へ戻し、`hasApiKey` の表示だけを更新する。
- `localStorage` へ保存しない。
- API クライアントは `console.*` を一切呼ばない。

`PUT` の `apiKey` は三状態である。**空のまま保存＝維持**（要求から省略）、**「消去」＝ `null` を送る**、
**入力あり＝設定**。

`PUT /api/settings/connection` は実行中だと **409 `runs-active`** を返す（PR10 決定 7。
`activeRunIds()` を見ている）。このとき**入力中の URL・API キー欄を消さず**、
「検査実行が走っているため変更できません」をそのまま出す。

接続先 URL は `GET`/`PUT /api/settings/connection` の応答としてしか画面に出さない。実行・原稿・
エラーの表示に出さない。

### 決定 19：テスト基盤

`packages/web/vite.config.ts` の `test.environment` を `node` → `jsdom` に変え、`test.setupFiles` に
`vitest.setup.ts` を指定する。setup では `@testing-library/jest-dom/vitest` を import し、
**`afterEach(cleanup)` を明示的に書く**。このリポジトリは `globals: true` を使わないため
（テストは `vitest` から明示 import する）、Testing Library の自動 cleanup は登録されない。書かないと
テスト間で DOM が残り、原因の分かりにくい失敗になる。

画面のテストは `MemoryRouter` で包み、API クライアントには fake の `fetch` を渡す。
**実際の `fetch` は一度も呼ばない。**

### 決定 20：完了条件に `pnpm build` を含める

`pnpm check` は typecheck・lint・test だけで、vite の本番ビルドを含まない。react-router、
CSS Modules、静的配信を初めて本格的に組む PR なので、`pnpm check` と `pnpm build` の**両方**を
完了条件にする。CI（`.github/workflows/`）は既に `pnpm build` を実行しているが、ローカルで完了を
宣言する前にも通す。

Windows のローカル確認では、さらに `pnpm start` とブラウザからの直接アクセス（`/`、
`/settings/connection`、`/runs/:id` の直リンク）を確認する。

---

## 画面ごとの仕様

### `/` — 原稿（編集中）

入力方法は「貼り付け／ファイルを読み込む」のラジオで切り替える。原稿名は 1〜200 文字で、
ファイル選択時はファイル名を初期値に入れて編集可能にする。貼り付けは textarea、ファイルは
`input[type="file"]` と選んだファイル名・サイズの表示だけで、**中身は読まない**。

### `/` — 原稿（確定済み）

`POST` の応答をそのまま使い、原稿名・書記素数・確定日時・「確定後の本文は変更できない」を表示し、
先頭 500 書記素のプレビューと、開いたときだけ全文を描く折りたたみを置く（決定 17）。
「別の原稿を選ぶ」で編集中に戻る。

### `/` — 検査設定と開始

決定 12 の構成。「検査を開始する」を押したときの順序は次のとおり。

1. `connection.checkConnection(modelId)` を呼び、**戻り値**で決定 9 の条件を判定する
2. 通らなければ `POST /api/runs` を**投げず**、接続設定へのリンク付きでエラーを出す
3. 通れば `StartRunRequest` を組み立て、`startRunRequestSchema` で検証し、スナップショットに固定して
   `POST /api/runs`
4. `RunDto` を受け取ったら `navigate(runPath(run.id))`

無効化条件は「原稿が未確定」「モデルが未選択」の 2 つ。未選択のときは接続設定へのリンクを出す。

### `/settings/connection`

`GET` で現在の `endpointUrl` と `hasApiKey` を表示する。API キーは決定 18 の三状態。モデル一覧は
`check.models` から `llm` / `vlm` だけを出す（決定 9）。409 `runs-active` の扱いは決定 18。

### `/runs/:id`

決定 16 のとおり。

---

## テスト

各群に ID を付け、タスクから参照する。画面のテストは `MemoryRouter` で包み、API クライアントには
fake の `fetch` を渡す。**実際の `fetch` は一度も呼ばない。**

### W1：API クライアント（`api/client.test.ts`）

- W1-1 8 つの口それぞれについて、`fetch` に渡るメソッド・パス・本文が正しい。
- W1-2 `uploadManuscript` の `FormData` に `File` がそのまま載り、`Content-Type` を**指定していない**。
- W1-3 `checkConnection()` を引数なしで呼ぶと本文に `modelId` を入れない。
- W1-4 `getRun(id, { signal })` の `signal` が `fetch` に渡る。
- W1-5 応答に余分なキーがあると `ApiResponseError`（`.strict()` が拒む）。

### W2：例外の分類と解析順（`api/errors.test.ts`）

- W2-1 `response.text()` が reject → `ApiTransportError`。
- W2-2 `fetch` 自体が reject → `ApiTransportError`。
- W2-3 400 で `apiErrorSchema` に合う本文 → `ApiRequestError(400, code, message)`。
- W2-4 **400 で HTML 本文 → `ApiRequestError(400, "unknown", …)`。`ApiResponseError` にしない。**
- W2-5 500 で HTML 本文 → `ApiRequestError(500, "unknown", …)`。
- W2-6 200 で HTML 本文 → `ApiResponseError`。
- W2-7 200 で JSON だがスキーマ違反 → `ApiResponseError`。
- W2-8 `isStartOutcomeUnknown` の真理値表：`RunDto` 受領＝false、4xx（`code` 既知）＝false、
  **4xx（`code: "unknown"`）＝false**、**5xx＝true**、`ApiResponseError`＝true、`ApiTransportError`＝true。

### W3：`localStorage`（`storage/local.test.ts`）

- W3-1 保存 → 読み出しの往復。
- W3-2 JSON として壊れた値 → 既定値を返し、キーを消す。
- W3-3 JSON だがスキーマ違反 → 既定値を返し、キーを消す。
- W3-4 `getItem` が throw する環境 → 既定値。例外を外に出さない。
- W3-5 `setItem` が throw する環境 → 例外を外に出さない。
- W3-6 `storedRunSettingsSchema` が `RUN_SETTINGS_DEFAULTS` 由来の値を受け入れる。
- W3-7 許容語は `{ manuscriptVersionId, allowedWordsRaw }` の組で保存される。

### W4：接続 context とヘッダー（`app/connection-context.test.tsx`、`app/model-selection.test.ts`、`app/header.test.tsx`）

- W4-1 **確認 A → B の順に送り、B が先に解決したとき、後から来た A の結果で状態を上書きしない。**
- W4-2 新しい確認を始めると、前の要求に渡した `signal` が `aborted` になる。
- W4-3 アンマウントで進行中の要求の `signal` が `aborted` になる。
- W4-4 `AbortError` は画面のエラーとして表示されない。
- W4-5 接続に失敗した → 保存済みの選択を**維持**する。
- W4-6 一覧は取れたが ID が無い → 選択を解除する。
- W4-7 ID はあるが種別が `embeddings` → 選択を解除する。
- W4-8 `llm` だが未ロード → 選択を**維持**し、ロードの案内を出す。
- W4-9 ロード済みの `llm` → 選択を維持し、開始できる。
- W4-10 `canStartWithModel` は `models` に該当 ID が無いとき false（`find` の `undefined` で落ちない）。
- W4-11 ヘッダーに最終確認時刻が出る。

### W5：接続設定画面（`features/connection/connection-settings-page.test.tsx`）

- W5-1 API キー欄が空のまま保存 → 要求本文に `apiKey` を**含めない**（維持）。
- W5-2 入力して保存 → `apiKey` が文字列で入る。
- W5-3 「消去」→ `apiKey: null`。
- W5-4 保存成功後に入力欄が空になり、`hasApiKey` の表示が更新される。
- W5-5 **409 `runs-active` → URL と API キー欄の入力が残り、メッセージが出る。**
- W5-6 `models` に `embeddings` と種別 `null` が混ざっていても選択肢に出ない。
- W5-7 未ロードの `llm` を選ぶと「LM Studio でロードしてください」が出る。

### W6：原稿（`features/manuscript/*.test.tsx`）

- W6-1 本文が空だと「この原稿を確定する」が `disabled`。
- W6-2 **空白だけの本文は確定できる**（`trim()` しない）。
- W6-3 原稿名が空、または 201 文字だと `disabled`。
- W6-4 ファイル経路：`FormData` に選んだ `File` がそのまま載る。**`File.text()` を呼ばない。**
- W6-5 400 `invalid-utf8` → 文字コードの問題として表示する（黙って置換しない）。
- W6-6 **プレビューが書記素境界で切れる**：499 / 500 / 501 書記素、および 500 番目付近に ZWJ 絵文字・
  異体字セレクタ・CRLF を置いた本文で、切り出しが壊れない。
- W6-7 本文が 500 書記素以下なら全文展開の操作を出さない。
- W6-8 **折りたたみを開く前は 501 書記素目以降が DOM に存在しない。**
- W6-9 起動時の復元：`GET /api/manuscripts/:id` が 404 → キーを消し、編集中に戻る。
- W6-10 起動時の復元：500 と通信失敗 → **キーを残し**、再試行できるエラーを出す。
- W6-11 「別の原稿を選ぶ」で編集中に戻る。削除の要求は出さない。

### W7：検査設定と開始（`features/settings/*.test.tsx`）

- W7-1 初期値が `RUN_SETTINGS_DEFAULTS` と一致する（タイムアウトは秒、丸め許容はパーセントで表示）。
- W7-2 タイムアウト 300 秒 → 送信は `checkMs: 300000`。
- W7-3 丸め許容 20 → 送信は `roundingTolerance: 0.2`。
- W7-4 観点を両方は外せない。
- W7-5 未ロードのモデルでは `POST /api/runs` を投げない（`checkConnection` だけが呼ばれる）。
- W7-6 `embeddings` でも投げない。
- W7-7 `validateChunkSettings` に反する値ではフォーム全体のエラーを出し、投げない。
- W7-8 開始で `checkConnection` → `startRun` の順に呼ばれる。
- W7-9 成功で `/runs/:id` へ遷移する。
- W7-10 送信中はボタンが `disabled`。
- W7-11 **5xx → 同じ `startOperationId` と同じ本文で再送できる。**
- W7-12 **2xx の契約違反 → 同上。**
- W7-13 **本文の読み取り失敗 → 同上。**
- W7-14 **4xx → 押し直すと新しい `startOperationId` になる。**
- W7-15 **再試行の前にフォームを変えても、送るのは最初のスナップショット。**
- W7-16 許容語は同じ原稿版でだけ復元され、別の原稿版では空になる。
- W7-17 原稿が未確定、またはモデル未選択のとき「検査を開始する」が `disabled`。

### W8：受付画面（`features/run-receipt/run-receipt-page.test.tsx`）

- W8-1 **初回も `GET /api/runs/:id` を 1 回呼ぶ**（`POST` の応答を画面へ渡さない）。
- W8-2 `RunStatus` 5 種すべてのラベル。
- W8-3 `RunStopReason` 8 種すべてのラベル。
- W8-4 `stopped` かつ `settings` → 「検査は開始できませんでした」と `stopMessage` と設定へ戻るリンク。
- W8-5 404 → 「その実行はありません」。
- W8-6 `progress` と `targets` を表示しない。
- W8-7 「最新の状態を取得」で 2 回目の `GET` が走る。

### W9：ルーティングと漏えい（`App.test.tsx`、`leak.test.tsx`）

- W9-1 `/`・`/settings/connection`・`/runs/:id` がそれぞれ描画される。
- W9-2 未知のパスで画面内 404 が出る。
- W9-3 `Link` で遷移し、履歴の戻る・進むが効く。
- W9-4 **API キーを入力しても、`PUT /api/settings/connection` 以外の要求本文に現れない。**
- W9-5 **API キーがパスワード欄の値以外のテキストとして描画されない。**
- W9-6 **API キーが `localStorage` に書かれない。**
- W9-7 **接続先 URL が接続設定画面以外に描画されない。**

W2-4、W4-1、W6-6、W6-8、W7-11〜15、W9-4〜7 は実装中に壊れやすく、本書の設計判断そのものを守る。

## 完了条件

- `pnpm check`（typecheck・lint・test）が通る。
- `pnpm build`（vite の本番ビルド）が通る。
- CI が Ubuntu・Windows とも緑になる。
- **マージ前に**ネイティブ Windows で `docs/guides/windows-verification.md` の 2 節（起動、静的配信、
  SPA の直リンク、LM Studio 接続、短い合成原稿で 1 回開始、Ctrl+C での終了と再起動）を通す。
- 仕様 11 節 1・2 項の UI 側が満たされる（1 項は上の Windows 確認での実検査で満たす）。

## 付随して直すもの

- **`docs/guides/windows-verification.md`（改訂済み）。** 「CI が毎回確認していること」「Windows で
  人が確認すること」「診断のときだけ使う手順」の 3 節に整理し直し、期待値を PR11 の画面に合わせた。
  本 PR の実装が終わったら、記述と実物が合っているかを確認する。
- **`docs/plans/2026-09-07-mvp-roadmap.md`（改訂済み）。** チェックポイントの位置を PR11 の前から
  PR11 のマージ条件へ移した。
- **`docs/decisions/0002-scaffold-conventions.md` の「検証状況」。** Windows の確認結果を追記する。
- **`README.md` の「現在の状態」。** PR10 のときと同様に更新する。
- **`packages/web/src/App.tsx`。** scaffold の書記素計数の画面を置き換える。`/api/health` の確認は
  Windows のガイドから `curl` で行うので、画面には残さない。

## 既知の制限（設計書に残す）

- **送信中に再読み込みすると、開始結果を復元できない。** 仕様 8.2 は二重送信の例として「複数タブ、
  連打など」を挙げており、再読み込みを明示的に除外しているわけではない。**本設計では開始要求を
  永続化せず、送信中の再読み込みでは結果を復元できない制限を許容する**（要求一式を `localStorage` に
  置く価値は MVP では無いと判断した）。
- **ヘッダーの接続状態は「最後に確認した時点」のもの。** ポーリングしないので、LM Studio 側の
  ロード状態は表示後に変わりうる（決定 8）。

---

## タスク分解

> **エージェント向け**：superpowers:subagent-driven-development で 1 タスクずつ実装する。各タスクは
> 「失敗するテストを書く → 落ちるのを見る → 最小の実装 → 通す → コミット」を単位とし、完了時に
> `pnpm check` を通す。本書の決定 1〜20 と「画面ごとの仕様」「テスト」節が要件の正本で、タスクはその
> 割り付けである。相対 import は `.ts` / `.tsx` 拡張子付き。識別子は英語、コメント・コミットメッセージ
> は日本語。`git add -A` を使わず、変更したファイルを明示して stage する。

順序と依存：

```
Task 1（shared のモデル種別）→ Task 2（web の土台とルーティング）
  → Task 3（API クライアント）→ Task 4（localStorage）
  → Task 5（接続 context とヘッダー）→ Task 6（接続設定画面）
  → Task 7（原稿）→ Task 8（検査設定と開始）→ Task 9（受付画面）
  → Task 10（漏えい検査とドキュメント）
```

Task 3 と Task 4 は互いに独立だが、**並列に実装しない**。Task 8 は Task 5・7 の両方に依存する。

### Task 1：`isGenerationCapable` と `LOADED_STATE` を shared へ移す（決定 9）

**Files**
- Create: `packages/shared/src/run/model-capability.ts`
- Create: `packages/shared/src/run/model-capability.test.ts`
- Modify: `packages/shared/src/index.ts`（再エクスポート）
- Modify: `packages/server/src/lmstudio/models.ts`（実体を消して shared からの再エクスポートにする）
- Modify: `packages/server/src/lmstudio/types.ts`（`LOADED_STATE` を shared からの再エクスポートにする）

**Interfaces（後続タスクが使う）**

```ts
/** `state` がこの値のときだけ生成を許す。 */
export const LOADED_STATE = "loaded";

/** ロード済みで、かつ種別が生成（chat）に使える（llm・vlm）ことを判定する。 */
export function isGenerationCapable(model: {
  readonly state: string | null;
  readonly type: string | null;
}): boolean;
```

構造的な引数にするのは、server の `ModelInfo`（`lmstudio/types.ts`）と shared の `ModelInfoDto`
（`api/dto.ts`）の両方をそのまま渡せるようにするためである。どちらも `state` / `type` が `string | null`。

**手順**

1. `model-capability.test.ts` を書く。`{state:"loaded",type:"llm"}` → true、`vlm` → true、
   `embeddings` → false、`{state:"not-loaded",type:"llm"}` → false、`state:null` → false、
   `type:null` → false、未知の `state:"loading"` → false。
2. `pnpm --filter @shuten/shared test` で落ちるのを見る（モジュールが無い）。
3. `model-capability.ts` を実装する。中身は現在の
   `packages/server/src/lmstudio/models.ts` と同じ式（`state === LOADED_STATE && (type === "llm" || type === "vlm")`）。
   `LOADED_STATE` もこのファイルに置く。
4. `packages/shared/src/index.ts` に `export { isGenerationCapable, LOADED_STATE } from "./run/model-capability.ts";` を足す。
5. `packages/server/src/lmstudio/models.ts` を
   `export { isGenerationCapable } from "@shuten/shared";` だけにする。
   `packages/server/src/lmstudio/types.ts` の `LOADED_STATE` の定義を
   `export { LOADED_STATE } from "@shuten/shared";` に置き換える（`client.ts` と `api/settings.ts` の
   import 元は変えない）。
6. `pnpm check`。既存の server のテスト（`lmstudio/models.test.ts` など）がそのまま通ること。
7. コミット。

**完了条件**：`pnpm check` が緑。server 側の import を 1 つも書き換えずに通る。

### Task 2：web の土台とルーティング（決定 2・3・4・5・19）

**Files**
- Modify: `packages/web/package.json`（決定 4 の依存を追加）
- Modify: `packages/web/vite.config.ts`（`test.environment` を `jsdom`、`test.setupFiles`）
- Create: `packages/web/vitest.setup.ts`
- Create: `packages/web/src/app/routes.ts`
- Create: `packages/web/src/app/layout.tsx`
- Create: `packages/web/src/app/not-found.tsx`
- Create: `packages/web/src/app/home-page.tsx`
- Create: `packages/web/src/features/connection/connection-settings-page.tsx`
- Create: `packages/web/src/features/run-receipt/run-receipt-page.tsx`
- Create: `packages/web/src/styles/tokens.css`、`packages/web/src/styles/global.css`
- Modify: `packages/web/src/main.tsx`（`<BrowserRouter>` で包む。`global.css` を import）
- Modify: `packages/web/src/App.tsx`（scaffold を捨てて `<Routes>` にする）
- Test: `packages/web/src/App.test.tsx`（W9-1〜3）

**Interfaces（後続タスクが使う）**

```ts
// app/routes.ts
export const ROUTES = {
  home: "/",
  connectionSettings: "/settings/connection",
  run: "/runs/:id",
} as const;
export function runPath(id: string): string; // `/runs/${encodeURIComponent(id)}`
```

3 つの画面コンポーネント（`HomePage` / `ConnectionSettingsPage` / `RunReceiptPage`）は本タスクでは
見出しだけの最小の中身にし、Task 6〜9 が実装で埋める。

**手順**

1. `pnpm --filter @shuten/web add react-router@8.3.1` と、`-D` で
   `@testing-library/dom@10.4.1` `@testing-library/jest-dom@7.0.1` `@testing-library/react@16.3.3`
   `@testing-library/user-event@14.6.7` `jsdom@30.0.1` を足す。**版は範囲指定にせず完全固定**
   （`package.json` に `^` や `~` が入っていないことを確認する）。
2. `vitest.setup.ts` を作る。

   ```ts
   import "@testing-library/jest-dom/vitest";
   import { cleanup } from "@testing-library/react";
   import { afterEach } from "vitest";

   // このリポジトリは globals: true を使わないので、自動 cleanup が登録されない（決定 19）。
   afterEach(() => {
     cleanup();
   });
   ```

3. `vite.config.ts` の `test` を次にする。`server.proxy` はそのまま残す。

   ```ts
   test: {
     name: "web",
     environment: "jsdom",
     setupFiles: ["./vitest.setup.ts"],
   },
   ```

4. `App.test.tsx` を書く（W9-1〜3）。`MemoryRouter` の `initialEntries` を
   `["/"]` / `["/settings/connection"]` / `["/runs/abc"]` / `["/nope"]` にして、それぞれの見出しが
   出ること。`Link` での遷移と `history.back()` 相当（`user-event` のクリック → `MemoryRouter` の
   履歴）で戻れること。
5. 落ちるのを見る。
6. `routes.ts`、`layout.tsx`（`<header>` の枠と `<main>`）、`not-found.tsx`、3 つの画面の最小実装、
   `tokens.css`（色・余白・文字サイズのカスタムプロパティ）、`global.css`（リセットと共通レイアウト）
   を作る。`App.tsx` を `<Routes>` に、`main.tsx` を `<BrowserRouter>` に書き換える。
   scaffold の書記素計数の表示は消す。
7. `pnpm check` と `pnpm build`。
8. コミット。

**完了条件**：`pnpm check` と `pnpm build` が緑。W9-1〜3 が通る。`packages/web/package.json` の
依存に範囲指定が無い。

### Task 3：API クライアントと例外（決定 14・15）

**Files**
- Create: `packages/web/src/api/errors.ts`
- Create: `packages/web/src/api/client.ts`
- Create: `packages/web/src/api/context.tsx`（クライアントを画面へ配る context）
- Test: `packages/web/src/api/errors.test.ts`（W2）、`packages/web/src/api/client.test.ts`（W1）

**Interfaces（後続タスクが使う）**

```ts
// api/errors.ts
export class ApiRequestError extends Error {
  constructor(readonly status: number, readonly code: string, message: string);
}
export class ApiResponseError extends Error {}
export class ApiTransportError extends Error {}

/** 開始要求の結果が不明か（決定 15）。同じ startOperationId で再送してよいかの判定に使う。 */
export function isStartOutcomeUnknown(error: unknown): boolean;

// api/client.ts
export interface ApiClient {
  getConnection(): Promise<ConnectionSettingsDto>;
  putConnection(body: PutConnectionRequest): Promise<ConnectionSettingsDto>;
  checkConnection(modelId?: string, options?: { signal?: AbortSignal }): Promise<ConnectionCheckDto>;
  createManuscript(body: CreateManuscriptRequest): Promise<ManuscriptVersionDto>;
  uploadManuscript(input: { file: File; name: string }): Promise<ManuscriptVersionDto>;
  getManuscript(id: string, options?: { signal?: AbortSignal }): Promise<ManuscriptVersionDto>;
  startRun(body: StartRunRequest): Promise<RunDto>;
  getRun(id: string, options?: { signal?: AbortSignal }): Promise<RunDetailDto>;
}
export function createApiClient(deps?: { fetch?: typeof globalThis.fetch }): ApiClient;

// api/context.tsx
export function ApiClientProvider(props: { client: ApiClient; children: ReactNode }): JSX.Element;
export function useApiClient(): ApiClient;
```

`createApiClient()` は `App.tsx` で **1 回だけ**呼び、`ApiClientProvider` で配る（Task 5）。
画面のテストはこの provider に fake の `fetch` で作ったクライアントを渡す。

**手順**

1. `errors.test.ts` を書く（W2-1〜8）。fake の `Response` は
   `new Response(body, { status })` で作れるが、**本文の読み取り失敗**は
   `{ ok, status, text: () => Promise.reject(new Error()) }` の形の最小オブジェクトで作る。
2. 落ちるのを見る。
3. `errors.ts` に 3 つの例外クラスと `isStartOutcomeUnknown` を実装する。判定は決定 15 の表のとおり。

   ```ts
   export function isStartOutcomeUnknown(error: unknown): boolean {
     if (error instanceof ApiTransportError) return true;
     if (error instanceof ApiResponseError) return true;
     if (error instanceof ApiRequestError) return error.status >= 500;
     return true; // 素性の分からない例外は「不明」に倒す
   }
   ```

4. `client.test.ts` を書く（W1-1〜5）。
5. 落ちるのを見る。
6. `client.ts` と `context.tsx` を実装する。本文の扱いは**決定 14 のコードそのまま**（読み取り → 解析 → 状態コードの
   順。`JSON.parse` の失敗をその場で投げない）。応答の検証は `@shuten/shared` の zod スキーマ。
   `uploadManuscript` は `FormData` に `file` と `name` を入れ、`Content-Type` を**指定しない**。
   `console.*` を呼ばない。例外に要求本文を入れない（決定 18）。
7. `pnpm check`。
8. コミット。

**完了条件**：`pnpm check` が緑。W1・W2 が通る。`client.ts` に PR12 用の口（一覧・停止・再開・
再試行・指摘・SSE）が 1 つも無い。

### Task 4：`localStorage` の包み（決定 6・7）

**Files**
- Create: `packages/web/src/storage/keys.ts`
- Create: `packages/web/src/storage/local.ts`
- Test: `packages/web/src/storage/local.test.ts`（W3）

**Interfaces（後続タスクが使う）**

```ts
// storage/keys.ts
export const STORAGE_KEYS = {
  selectedModelId: "shuten.v1.selectedModelId",
  runSettings: "shuten.v1.runSettings",
  allowedWords: "shuten.v1.allowedWords",
  manuscriptVersionId: "shuten.v1.manuscriptVersionId",
} as const;

// storage/local.ts
/** 壊れた値・スキーマ違反はキーを消して fallback を返す。localStorage の例外は外に出さない。 */
export function readStored<T>(key: string, schema: z.ZodType<T>, fallback: T): T;
export function writeStored(key: string, value: unknown): void;
export function removeStored(key: string): void;

export const storedRunSettingsSchema: z.ZodType<StoredRunSettings>;
export const storedAllowedWordsSchema: z.ZodType<StoredAllowedWords>;

export interface StoredRunSettings {
  readonly generation: { maxTokens: number; temperature: number; seed?: number; reasoningEffort: ReasoningEffort };
  readonly chunkSettings: ChunkSettings;
  readonly timeouts: { checkMs: number; recheckMs: number };
  readonly perspectives: readonly Perspective[];
  readonly recheckEnabled: boolean;
}
export interface StoredAllowedWords {
  readonly manuscriptVersionId: string;
  readonly allowedWordsRaw: string;
}
```

`storedRunSettingsSchema` は `startRunRequestSchema` を流用しない（決定 6。`startOperationId` /
`manuscriptVersionId` / `modelId` / `allowedWordsRaw` を含まない別の形である）。

**手順**

1. `local.test.ts` を書く（W3-1〜7）。`localStorage` が throw する環境は、`vi.spyOn(Storage.prototype, "getItem").mockImplementation(() => { throw new Error(); })` で作る。
2. 落ちるのを見る。
3. `keys.ts` と `local.ts` を実装する。`readStored` は
   「`getItem` を try/catch → `null` なら fallback → `JSON.parse` を try/catch → `schema.safeParse` →
   失敗なら `removeStored` して fallback」。
4. `pnpm check`。
5. コミット。

**完了条件**：`pnpm check` が緑。W3 が通る。`local.ts` が API キーを扱う経路を持たない。

### Task 5：接続 context とヘッダー（決定 8・9・10）

**Files**
- Create: `packages/web/src/app/model-selection.ts`
- Create: `packages/web/src/app/connection-context.tsx`
- Create: `packages/web/src/app/header.tsx`、`packages/web/src/app/header.module.css`
- Modify: `packages/web/src/App.tsx`（`createApiClient()` を 1 回だけ呼び、
  `<ApiClientProvider>` → `<ConnectionProvider>` の順で `<Routes>` を包む）
- Modify: `packages/web/src/app/layout.tsx`（`<Header />` を置く）
- Test: `packages/web/src/app/model-selection.test.ts`（W4-5〜10）、
  `packages/web/src/app/connection-context.test.tsx`（W4-1〜4）、
  `packages/web/src/app/header.test.tsx`（W4-11）

**Interfaces（後続タスクが使う）**

```ts
// app/model-selection.ts
/** 開始してよいか（決定 9）。models に該当 ID が無ければ false。 */
export function canStartWithModel(check: ConnectionCheckDto, modelId: string | null): boolean;

/** 接続確認の結果を受けて、保存済みの選択を維持するか解除するかを決める（決定 6 の表）。 */
export function nextSelectedModelId(
  check: ConnectionCheckDto,
  stored: string | null,
): string | null;

// app/connection-context.tsx
export interface ConnectionState {
  readonly check: ConnectionCheckDto | null;
  readonly checkedAt: Date | null;
  readonly selectedModelId: string | null;
  readonly error: string | null;   // AbortError はここに入れない（決定 10）
  readonly checking: boolean;
}
export interface ConnectionApi extends ConnectionState {
  /** 接続確認を投げ、結果をそのまま返す。呼び出し側は戻り値で判定する（決定 10）。 */
  checkConnection(modelId?: string): Promise<ConnectionCheckDto>;
  selectModel(modelId: string): void;
}
export function ConnectionProvider(props: { client: ApiClient; children: ReactNode }): JSX.Element;
export function useConnection(): ConnectionApi;
```

**手順**

1. `model-selection.test.ts` を書く（W4-5〜10）。決定 6 の表の 5 行と、`models` に該当 ID が無い場合。
2. 落ちるのを見る。
3. `model-selection.ts` を実装する。`canStartWithModel` は決定 9 のコードそのまま
   （`find` の結果を `undefined` でガードしてから `isGenerationCapable` に渡す）。
   `nextSelectedModelId` は決定 6 の表のとおり（**接続失敗は維持**、ID なしと種別違いは解除、
   未ロードの `llm`/`vlm` は維持）。
4. `connection-context.test.tsx` を書く（W4-1〜4）。W4-1 は、fake の `fetch` が
   1 回目は遅く 2 回目は速く解決するようにして、**先に解決した 2 回目の結果が残る**ことを見る。
   W4-2・W4-3 は `fetch` に渡された `signal` の `aborted` を見る。
5. 落ちるのを見る。
6. `connection-context.tsx` を実装する。世代番号（`useRef<number>`）で最新の応答だけを反映し、
   新しい要求のたびに前の `AbortController` を `abort()` し、アンマウントでも `abort()` する。
   `AbortError` は `error` に入れない。`checkConnection` は**必ず結果を返す**（context の更新を
   待たずに呼び出し側が判定できるようにする。決定 10）。
   起動時に 1 回、保存済みモデル ID を添えて確認し、`nextSelectedModelId` の結果を
   `STORAGE_KEYS.selectedModelId` に書き戻す。
7. `header.test.tsx`（W4-11）を書いて落ちるのを見てから、`header.tsx` を実装する。接続状態・選択モデル・**最終確認時刻**・「再確認」・接続設定へのリンク。
   「最後に確認した時点」と分かる表現にする（決定 8）。接続先 URL は**出さない**（決定 18）。
8. `pnpm check`。
9. コミット。

**完了条件**：`pnpm check` が緑。W4 が通る。ヘッダーに接続先 URL が出ない。定期的な再確認
（`setInterval` / ポーリング）が 1 か所も無い。

### Task 6：接続設定画面（決定 9・18）

**Files**
- Modify: `packages/web/src/features/connection/connection-settings-page.tsx`（Task 2 の最小実装を置き換える）
- Create: `packages/web/src/features/connection/connection-settings-page.module.css`
- Test: `packages/web/src/features/connection/connection-settings-page.test.tsx`（W5）

**Interfaces**
- Consumes: `useConnection()`（Task 5）、`ApiClient.getConnection` / `putConnection`（Task 3）、
  `isGenerationCapable`（Task 1）。
- Produces: なし（画面のみ）。

**手順**

1. `connection-settings-page.test.tsx` を書く（W5-1〜7）。
2. 落ちるのを見る。
3. 実装する。要点は 4 つ。
   - **API キーは三状態**（決定 18）。`input[type="password"]` を空のままなら要求に `apiKey` を
     入れない（維持）、「消去」なら `apiKey: null`、入力があればその文字列。保存成功後は入力欄を
     空に戻し、`hasApiKey` の表示だけを更新する。
   - **409 `runs-active`** を受けたら、入力中の URL・API キー欄の値を**消さず**にメッセージを出す。
   - モデル一覧は `check.models` から `type` が `"llm"` / `"vlm"` のものだけを出す。未ロードのものも
     選べるが「LM Studio でロードしてください」を添える（決定 9）。
   - 保存に成功したら `useConnection().checkConnection()` を呼ぶ（決定 8 の契機 2）。
4. `pnpm check`。
5. コミット。

**完了条件**：`pnpm check` が緑。W5 が通る。API キーが `localStorage` に書かれる経路が無い。

### Task 7：原稿（決定 6・11・17）

**Files**
- Create: `packages/web/src/features/manuscript/manuscript-editor.tsx`（編集中）
- Create: `packages/web/src/features/manuscript/manuscript-confirmed.tsx`（確定済み）
- Create: `packages/web/src/features/manuscript/preview.ts`（書記素境界での切り出し）
- Create: `packages/web/src/features/manuscript/use-manuscript.ts`（2 状態と起動時の復元）
- Create: `packages/web/src/features/manuscript/manuscript.module.css`
- Modify: `packages/web/src/app/home-page.tsx`（原稿の節を置く）
- Test: `packages/web/src/features/manuscript/preview.test.ts`（W6-6〜7）、
  `packages/web/src/features/manuscript/manuscript.test.tsx`（W6-1〜5、W6-8〜11）

**Interfaces（Task 8 が使う）**

```ts
// features/manuscript/preview.ts
export const PREVIEW_GRAPHEMES = 500;
/** 先頭 PREVIEW_GRAPHEMES 書記素。本文が短ければ全文（RangeError を出さない。決定 17）。 */
export function buildPreview(body: string): { text: string; truncated: boolean };

// features/manuscript/use-manuscript.ts
export type ManuscriptState =
  | { readonly kind: "editing" }
  | { readonly kind: "confirmed"; readonly version: ManuscriptVersionDto };
export interface ManuscriptApi {
  readonly state: ManuscriptState;
  readonly restoreError: string | null;
  confirmPaste(input: { name: string; body: string }): Promise<void>;
  confirmFile(input: { name: string; file: File }): Promise<void>;
  reset(): void;   // 「別の原稿を選ぶ」。選択解除だけで削除はしない
}
export function useManuscript(client: ApiClient): ManuscriptApi;
```

**手順**

1. `preview.test.ts` を書く（W6-6〜7）。499 / 500 / 501 書記素の本文、500 番目付近に ZWJ 絵文字
   （`"👨‍👩‍👧"`）・異体字セレクタ・CRLF を置いた本文。**本文が 500 書記素未満でも例外にならない**
   ことを必ず入れる（`offsetAt` の `RangeError` を踏むのはここ）。
2. 落ちるのを見る。
3. `preview.ts` を実装する。決定 17 のコードそのまま（`Math.min(index.count, PREVIEW_GRAPHEMES)`）。
4. `manuscript.test.tsx` を書く（W6-1〜5、W6-8〜11）。W6-4 は `File` の `text` を
   `vi.spyOn` して**呼ばれないこと**を見る。W6-8 は折りたたみを開く前に 501 書記素目以降の文字列が
   `document.body.textContent` に無いことを見る。
5. 落ちるのを見る。
6. 実装する。要点は 5 つ。
   - 入力方法は貼り付けとファイルのラジオ。**ファイルの中身は確定まで読まない。**
   - 確定ボタンの無効化は決定 11 の 3 条件（本文が `length === 0`、ファイル未選択、原稿名が空か
     201 文字以上）。**`trim()` を使わない。**
   - 確定は `createManuscript` か `uploadManuscript`。ファイルは `File` をそのまま送る。
   - 確定済みは原稿名・`countGraphemes(body)`・確定日時・「確定後の本文は変更できない」と、
     プレビュー＋**開いたときだけ全文を描く**折りたたみ（`useState` で開閉を持つ。`<details>` に
     全文を入れっぱなしにしない）。
   - 起動時の復元は `STORAGE_KEYS.manuscriptVersionId` → `getManuscript`。**404 ならキーを消して
     編集中へ、5xx と通信失敗ならキーを残して再試行できるエラー**（決定 6）。
7. `pnpm check`。
8. コミット。

**完了条件**：`pnpm check` が緑。W6 が通る。`File.text()` と `FileReader` を 1 か所も使っていない。

### Task 8：検査設定と開始（決定 7・12・13・15）

**Files**
- Create: `packages/web/src/features/settings/run-settings-form.tsx`
- Create: `packages/web/src/features/settings/units.ts`（画面と API の単位変換）
- Create: `packages/web/src/features/settings/use-start-run.ts`（開始操作とスナップショット）
- Create: `packages/web/src/features/settings/settings.module.css`
- Modify: `packages/web/src/app/home-page.tsx`（設定の節と開始ボタンを置く）
- Test: `packages/web/src/features/settings/units.test.ts`（W7-2〜3）、
  `packages/web/src/features/settings/run-settings-form.test.tsx`（W7-1、W7-4、W7-7、W7-16、W7-17）、
  `packages/web/src/features/settings/use-start-run.test.tsx`（W7-5〜6、W7-8〜15）

**Interfaces**

```ts
// features/settings/units.ts
export function msToSeconds(ms: number): number;        // 300000 → 300
export function secondsToMs(seconds: number): number;   // 300 → 300000
export function toleranceToPercent(value: number): number; // 0.2 → 20
export function percentToTolerance(percent: number): number; // 20 → 0.2
export const MAX_TIMEOUT_SECONDS: number; // Math.floor(MAX_TIMEOUT_MS / 1000)

// features/settings/use-start-run.ts
export type StartOutcome =
  | { readonly kind: "idle" }
  | { readonly kind: "sending" }
  | { readonly kind: "failed"; readonly message: string; readonly retryable: boolean }
  | { readonly kind: "started"; readonly runId: string };
export interface StartRunApi {
  readonly outcome: StartOutcome;
  /** 新しい開始操作。startOperationId を作り、要求全体をスナップショットに固定する。 */
  start(request: Omit<StartRunRequest, "startOperationId">): Promise<void>;
  /** 結果不明からの再試行。同じ ID と同じスナップショットを再送する（決定 15）。 */
  retry(): Promise<void>;
}
export function useStartRun(deps: { client: ApiClient; connection: ConnectionApi }): StartRunApi;
```

**手順**

1. `units.test.ts` を書く（W7-2〜3）。`300000 → 300 → 300000`、`0.2 → 20 → 0.2`、`0.15 → 15 → 0.15`。
2. 落ちるのを見る。`units.ts` を実装する。
3. `run-settings-form.test.tsx` を書く（W7-1、W7-4、W7-7、W7-16、W7-17）。
4. 落ちるのを見る。フォームを実装する。仕様 5.2 の 6 項目を既定表示、残りは折りたたみの
   **「詳細設定」**（決定 12）。初期値は `RUN_SETTINGS_DEFAULTS`、思考は `none`、`seed` は空欄なら
   送らない。タイムアウトは**秒（1〜`MAX_TIMEOUT_SECONDS`）**、丸め許容は**パーセント（0〜99）**で
   入力させる。許容語は `storedAllowedWordsSchema` の組で保存し、**同じ原稿版のときだけ復元**する
   （決定 7）。既定値から変えた項目があれば、折りたたみを閉じたままでも分かるようにする。
5. `use-start-run.test.tsx` を書く（W7-5〜6、W7-8〜15）。W7-15 は、`start` を呼んで 5xx を返し、
   フォームの値を変えてから `retry()` を呼び、**2 回目の要求本文が 1 回目と完全に一致する**ことを見る。
6. 落ちるのを見る。
7. `use-start-run.ts` を実装する。順序は決定 13 の 4 段（`checkConnection` の**戻り値**で
   `canStartWithModel` を判定 → 通らなければ投げない → `StartRunRequest` を組んで
   `startRunRequestSchema.safeParse` → `validateChunkSettings` を呼ぶ → スナップショットに固定して
   `startRun`）。`validateChunkSettings` の `InvalidChunkSettingsError` はフォーム全体のエラーに写す。
   失敗は `isStartOutcomeUnknown` で分け、**不明なら ID とスナップショットを保持**、
   **確定なら破棄**する（決定 15）。送信中はボタンを `disabled` にする。
   `startOperationId` は `crypto.randomUUID()`。
8. `home-page.tsx` で原稿（Task 7）と設定をつなぎ、成功したら `navigate(runPath(runId))`。
   原稿が未確定、またはモデル未選択のとき開始ボタンを `disabled` にする。
9. `pnpm check`。
10. コミット。

**完了条件**：`pnpm check` が緑。W7 が通る。`validateHardTimeouts` をクライアントで呼んでいない
（サーバー設定に依存するため。決定 13）。

### Task 9：受付画面（決定 1・16）

**Files**
- Modify: `packages/web/src/features/run-receipt/run-receipt-page.tsx`（Task 2 の最小実装を置き換える）
- Create: `packages/web/src/features/run-receipt/labels.ts`
- Create: `packages/web/src/features/run-receipt/run-receipt.module.css`
- Test: `packages/web/src/features/run-receipt/labels.test.ts`（W8-2〜3）、
  `packages/web/src/features/run-receipt/run-receipt-page.test.tsx`（W8-1、W8-4〜7）

**Interfaces**

```ts
// features/run-receipt/labels.ts
export const RUN_STATUS_LABELS = { /* … */ } as const satisfies Record<RunStatus, string>;
export const RUN_STOP_REASON_LABELS = { /* … */ } as const satisfies Record<RunStopReason, string>;
```

`satisfies` を付けるのが要点である（決定 16）。列挙が増えたら型検査で落ちる。

**手順**

1. `labels.test.ts` を書く（W8-2〜3）。`RUN_STATUSES` と `RUN_STOP_REASONS` を回して、
   **すべての値にラベルがあり、空文字でない**ことを確認する。
2. 落ちるのを見る。`labels.ts` を実装する（決定 16 の表の 5 種と、停止理由 8 種）。
3. `run-receipt-page.test.tsx` を書く（W8-1、W8-4〜7）。
4. 落ちるのを見る。
5. 実装する。要点は 4 つ。
   - **初回も `GET /api/runs/:id` を 1 回だけ呼ぶ**（決定 1）。`location.state` を読まない。
   - 見出しは常に中立の「検査実行」。
   - `stopped` かつ `stopReason === "settings"` は「検査は開始できませんでした」に切り替え、
     `stopMessage` と `/` へ戻るリンクを出す（決定 16）。
   - `RunDetailDto` の `progress` と `targets` は**使わない**。404 は「その実行はありません」。
   - 「最新の状態を取得」だけを置く。`setInterval` も SSE も無い。
6. `pnpm check`。
7. コミット。

**完了条件**：`pnpm check` が緑。W8 が通る。`EventSource` と `setInterval` が web に 1 か所も無い。

### Task 10：漏えい検査とドキュメント（決定 18・20）

**Files**
- Create: `packages/web/src/leak.test.tsx`（W9-4〜7）
- Modify: `README.md`（「現在の状態」）
- Modify: `docs/guides/windows-verification.md`（2 節の期待値が実物と合っているかを確認して直す）
- Test: 上記

**手順**

1. `leak.test.tsx` を書く（W9-4〜7）。PR10 の `packages/server/src/api/leak.test.ts` と同じ趣旨で、
   web 側から見る。
   - 接続設定画面で API キーを入力して保存し、fake の `fetch` に渡った**すべての要求**を集めて、
     `PUT /api/settings/connection` 以外の本文に API キーの文字列が含まれないこと。
   - 保存後、`document.body.textContent` に API キーの文字列が現れないこと
     （パスワード欄の `value` は対象外。`input[type="password"]` の値としては存在してよい）。
   - `localStorage` のすべてのキーの値に API キーの文字列が含まれないこと。
   - 接続設定画面以外（`/`、`/runs/:id`）の `document.body.textContent` に接続先 URL が現れないこと。
2. 落ちるなら直す（ここまでのタスクの実装が正しければ通るはず。**通らなかったら実装を直す。
   テストを緩めない**）。
3. `README.md` の「現在の状態」を更新する。PR10 の記述に、画面から接続設定・原稿の確定・検査の開始が
   できるようになったことを足す。**接続先 URL の実例や機器固有の値を書かない。**
4. `docs/guides/windows-verification.md` の 2 節のチェックリストを、実装した画面の実際の文言・
   遷移と突き合わせて直す。
5. `pnpm check` と `pnpm build`。
6. コミット。

**完了条件**：`pnpm check` と `pnpm build` が緑。W9-4〜7 が通る。README とガイドが実物と合っている。

### 実装後（マージ前）

1. PR を作り、CI が Ubuntu・Windows とも緑になることを確認する。
2. ネイティブ Windows で `docs/guides/windows-verification.md` の 2 節を通す（**Ctrl+C と再起動を含む**）。
3. 問題があれば同じ PR で直す。
4. 結果を `docs/decisions/0002-scaffold-conventions.md` の「検証状況」に追記する。
5. マージはユーザーが行う。
