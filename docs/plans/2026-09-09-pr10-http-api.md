# PR10 詳細計画：HTTP API と SSE（server）

作成日：2026-09-09
状態：設計中（計画書のタスク部分は未記載）
仕様：`docs/spec/mvp-spec.md`（v0.9）4、5.1、5.2（初期値）、8.2（ブラウザを閉じても継続、再開と新規の区別、二重送信）、9（ループバック、テキストとして扱う）、11 節（2・12・13 項）
前提：`docs/plans/2026-09-07-mvp-roadmap.md` の PR10 節、`docs/reference/invariants.md`、
`docs/plans/2026-09-09-pr9-orchestration.md`（決定 1〜23）、`docs/plans/2026-09-09-pr9b-orchestrator.md`（決定 24〜45）

## 目標

PR9b までに関数として動いているオーケストレーター・永続化層・LM Studio クライアントを、
ブラウザ（PR11・PR12）から呼べる HTTP API と SSE にする。すなわち、

- `/api/` 配下の JSON API（接続設定と接続確認、原稿、実行の開始・停止・再開・再試行、指摘、採否、復旧確認）
- `/api/runs/:id/events` の SSE（進捗通知。状態の正本は DB）
- UI から接続先を上書きする `settings` 表と、実行中に接続先が変わらない仕組み
- graceful shutdown と `LmStudioClient.close()`（PR7・PR8 からの持ち越し）
- PR11・PR12 が import する要求・応答の型と zod スキーマ（`packages/shared/src/api/`）

本 PR の成果物は「画面が依存する契約」である。エンドポイントの一覧と DTO の形を本書で固定し、
PR11・PR12 はこれを読んで作る。

## 対象外（MUST NOT）

- **画面を作らない**（PR11・PR12）。
- **エクスポート（`GET /api/runs/:id/export`）を作らない**（PR13。決定 16）。
- **オーケストレーターの実行の意味を変えない。** 状態遷移表、復旧待ちの規則、再開・再試行の受け付け条件は
  PR9 の決定のまま。本 PR が足すのは判別子・接続の供給元・実行中判定の 3 点だけ（決定 6・7・10）。
- **接続断で `failed` になった単位を `pending` に戻さない**（決定 17。決定 20 の改訂は持ち越しのまま）。
- **API キーを DB に保存しない。応答・ログ・SSE・エラーメッセージに API キーを出さない。接続先 URL は
  `GET/PUT /api/settings/connection` の応答（利用者が設定を見る場所）にだけ出し、実行・指摘・単位・SSE・
  エラーの本文には出さない。**
- **本文を加工しない。** 貼り付けはブラウザから受け取った文字列をそのまま、ファイルは `ingestUtf8Bytes`
  （BOM 除外のみ）の結果をそのまま保存する。
- **本文を書き換える経路を作らない。** 採否は `judgments` への記録だけ。
- **サーバー側の絞り込み・ページングを作らない**（決定 15）。
- **サービス層を作らない**（決定 1）。
- **Hono RPC（`hc`）を使わない**（決定 2）。
- CORS を設定しない（vite の dev server が `/api` を proxy し、本番は同一オリジンで静的配信する）。

## 全体の制約（不変条件から）

- 待ち受けは既定で `127.0.0.1`（`config.ts` のまま）。
- API キーを検査履歴・ログ・結果出力に含めない。**接続先 URL は接続設定の応答以外に出さない**
  （`runs.endpoint_url` に保存するだけで、`RunDto` には載せない。PR9b 決定 24）。
- SSE は通知手段。状態の正本は DB。ブラウザ切断で検査を止めず、再接続時は API で状態を取得して復元する。
- 「再開」は既存の実行 ID の続き、「新規検査の開始」は常に新しい実行 ID。二重送信の防止は
  `startOperationId` の同一性で判定する（PR9b 決定 12）。
- LLM 生成要求はバックエンド全体で単一キュー、同時実行数 1。**HTTP 層は生成要求を送る経路を新設しない**
  （接続確認も試験生成を送らない。決定 8）。
- 未ロードのモデルに生成要求を送らない。
- 分割範囲と確定位置の正本はサーバーが保存した UTF-16 範囲。API はそれをそのまま返す。
- 失敗・形式不正を指摘ゼロに置き換えない。実行の `status` / `stopReason` と単位の `failure` を落とさずに返す。
- 依存の版は完全固定。相対 import は `.ts` 拡張子付き。scripts は Windows で動く書き方。
- 公開リポジトリ。個人情報・機器固有の値・実原稿をコミットしない。

## 作るもの

| ファイル | 内容 |
| --- | --- |
| `shared/src/api/dto.ts` | 応答の型（`RunDto` など。本書「DTO」節） |
| `shared/src/api/requests.ts` | 要求の zod スキーマと型（本書「要求」節） |
| `shared/src/api/events.ts` | SSE の `RunEventDto`（決定 12） |
| `shared/src/api/defaults.ts` | `RUN_SETTINGS_DEFAULTS`（決定 9） |
| `shared/src/run/status.ts` | `RUN_STATUSES` / `UNIT_STATUSES`（server から移す。決定 2） |
| `shared/src/run/judgment.ts` | `JUDGMENT_STATUSES`（同上） |
| `shared/src/run/stop-reason.ts` | `RunStopReason` / `RecheckNotApplicableReason` / `CandidateLocateStatus` / `FindingLocateStatus`（同上） |
| `shared/src/run/reasoning-effort.ts` | `ReasoningEffort`（同上） |
| `server/src/api/router.ts` | `createApiRouter(deps)`。下記ハンドラーを 1 つの `Hono` にまとめる |
| `server/src/api/errors.ts` | `ApiError` と `app.onError` の写像（決定 4） |
| `server/src/api/dto.ts` | レコード → DTO の射影（決定 3） |
| `server/src/api/settings.ts` | `GET/PUT /api/settings/connection`、`POST /api/settings/connection/check` |
| `server/src/api/manuscripts.ts` | `POST /api/manuscripts`、`POST /api/manuscripts/upload`、`GET /api/manuscripts/:id` |
| `server/src/api/runs.ts` | `GET /api/runs`、`POST /api/runs`、`GET /api/runs/:id`、`GET /api/runs/:id/units`、`stop` / `resume` / `retry-failed` |
| `server/src/api/run-view.ts` | `GET /api/runs/:id` の集約（実行 + 進捗 + 検査対象）と `units` の組み立て |
| `server/src/api/findings.ts` | `GET /api/runs/:id/findings`、`GET /api/findings/:id`、`PUT /api/findings/:id/judgment` |
| `server/src/api/recovery.ts` | `GET /api/recovery`、`POST /api/recovery/confirm` |
| `server/src/api/events.ts` | `GET /api/runs/:id/events`（SSE） |
| `server/src/connection.ts` | `ConnectionManager`（決定 5・6） |
| `server/src/run/event-hub.ts` | `createRunEventHub()`（決定 13） |
| `server/src/db/repositories/settings.ts` | `settings` 表の読み書き |
| `server/src/db/repositories/runs.ts` | `listRuns(db)` の追加（`manuscript_versions` と結合して原稿名を付け、`started_at` 降順） |
| `server/src/db/schema.ts`、`server/drizzle/` | `settings` 表の追加と生成 SQL |
| `server/src/lmstudio/client.ts` | `close()` の追加（決定 19） |
| `server/src/run/orchestrator.ts` | `rejectReason`、`ConnectionSource`、`activeRunIds()`（決定 6・7・10） |
| `server/src/app.ts` | `createApp(config, deps)` に変更。`/api/health` は残す |
| `server/src/index.ts` | 配線と graceful shutdown（決定 18） |
| `cli/src/args.ts` | `DEFAULTS` を `RUN_SETTINGS_DEFAULTS` 参照に寄せる（決定 9） |

## エンドポイント

すべて `/api/` 配下、要求・応答は JSON（アップロードのみ multipart、`events` のみ SSE）。
エラーは 1 形式 `{ error: { code: string; message: string } }`（決定 4）。

| メソッド・パス | 要求 | 応答 | 状態コード |
| --- | --- | --- | --- |
| `GET /api/settings/connection` | — | `ConnectionSettingsDto` | 200 |
| `PUT /api/settings/connection` | `PutConnectionRequest` | `ConnectionSettingsDto` | 200 / 400 `validation` / 409 `runs-active` |
| `POST /api/settings/connection/check` | `ConnectionCheckRequest` | `ConnectionCheckDto` | 200（到達不能も 200 で `reachable: false`） |
| `POST /api/manuscripts` | `CreateManuscriptRequest` | `ManuscriptVersionDto` | 201 / 400 `validation` `empty-body` `malformed-body` |
| `POST /api/manuscripts/upload` | multipart：`file`（バイト列）、`name` | `ManuscriptVersionDto` | 201 / 400 `validation` `invalid-utf8` `empty-body` `malformed-body` |
| `GET /api/manuscripts/:id` | — | `ManuscriptVersionDto` | 200 / 404 |
| `GET /api/runs` | — | `RunSummaryDto[]`（開始日時の降順） | 200 |
| `POST /api/runs` | `StartRunRequest` | `RunDto` | 201（新規）/ 200（同じ `startOperationId` の既存）/ 400 `validation` `invalid-chunk-settings` / 404 `not-found`（原稿版） |
| `GET /api/runs/:id` | — | `RunDetailDto` | 200 / 404 |
| `GET /api/runs/:id/units` | — | `RunUnitsDto` | 200 / 404 |
| `POST /api/runs/:id/stop` | — | `{ run: RunDto }` | 202 / 404 / 409 `run-not-active` |
| `POST /api/runs/:id/resume` | — | `{ run: RunDto }` | 202 / 404 / 409 `run-rejected-<reason>` |
| `POST /api/runs/:id/retry-failed` | `RetryFailedRequest` | `{ run: RunDto }` | 202 / 400 `validation` `invalid-retry-target` / 404 / 409 `run-rejected-<reason>` |
| `GET /api/recovery` | — | `RecoveryDto` | 200 |
| `POST /api/recovery/confirm` | `ConfirmRecoveryRequest` | `RecoveryDto` | 200 / 400 `validation` |
| `GET /api/runs/:id/findings` | — | `FindingDto[]`（`listFindings` の並び：本文位置 `start` 昇順、位置未確定は最後、同順位は `createdAt` → `id`） | 200 / 404 |
| `GET /api/findings/:id` | — | `FindingDetailDto` | 200 / 404 |
| `PUT /api/findings/:id/judgment` | `PutJudgmentRequest` | `JudgmentDto` | 200 / 400 `validation` / 404 |
| `GET /api/runs/:id/events` | — | SSE（決定 12） | 200 / 404 |
| `GET /api/health` | — | 既存のまま | 200 |

補足：

- `POST /api/runs` の 201 は**作成直後のスナップショット**である。`startRun` は生成要求を送らずに同期で
  返り、ループは次のマイクロタスクから走るので、モデル未ロードや復旧ゲート閉鎖による停止は 201 の時点では
  まだ `running` で、SSE か `GET /api/runs/:id` で分かる。201 の時点で終端状態なのは開始計画で判明するもの
  （入力上限超過など）だけ（決定 14）。
- `stop` / `resume` / `retry-failed` の 202 は「受け付けた」であり、実行の結末は SSE または
  `GET /api/runs/:id` で追う。
- 404 の判定はハンドラーが `findRun` / `findManuscriptVersion` / `findFinding` で行う（決定 10）。
- `POST /api/runs` の 200 / 201 の判別は、`startRun` を呼ぶ前に `findRunByStartOperationId` で既存の有無を
  見て決める（`startRun` の戻り値だけでは新規か既存かが分からない）。

## DTO（`shared/src/api/dto.ts`）

原則（決定 3）：

- 日時は ISO 8601 文字列（`Date.toISOString()`）。
- `RunRecord` → `RunDto` は `endpointUrl` と `startOperationId` を落とし、日時を文字列にするだけ。他は同名。
- レコードにあって DTO に無い項目は「内部でだけ使う値」である。DTO に足すときは本書を改訂する。

```ts
import type {
  ChunkSettings, DiagnosticCandidate, FailureReason, FindingCategory, InitialVerdict, LlmFinding,
  LocateFailureReason, Perspective, Range, RecheckReasonKind, RecheckVerdict,
} from "../index.ts";
import type { JudgmentStatus } from "../run/judgment.ts";
import type { ReasoningEffort } from "../run/reasoning-effort.ts";
import type { RunStatus, UnitStatus } from "../run/status.ts";
import type {
  CandidateLocateStatus, FindingLocateStatus, RecheckNotApplicableReason, RunStopReason,
} from "../run/stop-reason.ts";

/** 接続設定。キー本体は決して返さない（決定 5）。 */
export interface ConnectionSettingsDto {
  readonly endpointUrl: string;
  readonly hasApiKey: boolean;
}

/** `/api/v0/models` の 1 要素。server の `ModelInfo` と同形（欠けうる値は null）。 */
export interface ModelInfoDto {
  readonly id: string;
  readonly type: string | null;
  readonly state: string | null;
  readonly quantization: string | null;
  readonly maxContextLength: number | null;
  readonly loadedContextLength: number | null;
}

/** 接続確認の結果（決定 8）。到達不能でも 200 で返す。生成要求は送らない。 */
export interface ConnectionCheckDto {
  readonly reachable: boolean;
  /** `reachable: false` のときだけ非 null。`message` は `reason` ごとの定型文で、URL は入れない。 */
  readonly error: { readonly reason: FailureReason; readonly message: string } | null;
  readonly models: readonly ModelInfoDto[];
  /** 要求に `modelId` が無ければ null。 */
  readonly model: {
    readonly id: string;
    readonly found: boolean;
    readonly state: string | null;
    /** `state === "loaded"` と厳密に一致するときだけ true（`LOADED_STATE`）。 */
    readonly loaded: boolean;
  } | null;
}

export interface ManuscriptVersionDto {
  readonly id: string;
  readonly name: string;
  readonly body: string;
  readonly bodyHash: string;
  readonly createdAt: string;
}

export interface GenerationSettingsDto {
  readonly maxTokens: number;
  readonly temperature: number;
  readonly seed?: number | undefined;
  readonly reasoningEffort?: ReasoningEffort | undefined;
}

export interface RunDto {
  readonly id: string;
  readonly manuscriptVersionId: string;
  readonly modelId: string;
  readonly modelInfo: ModelInfoDto | null;
  readonly generationSettings: GenerationSettingsDto;
  readonly chunkSettings: ChunkSettings;
  readonly timeouts: { readonly checkMs: number; readonly recheckMs: number };
  readonly perspectives: readonly Perspective[];
  readonly recheckEnabled: boolean;
  readonly allowedWords: readonly string[];
  readonly allowedWordRuleVersion: string;
  readonly promptVersion: string;
  readonly diagnosticTransformVersion: string;
  readonly status: RunStatus;
  readonly stopReason: RunStopReason | null;
  readonly stopMessage: string | null;
  readonly generationUnconfirmed: boolean;
  readonly stopRequestedAt: string | null;
  readonly recoveryConfirmMs: number;
  readonly startedAt: string;
  readonly finishedAt: string | null;
}

export interface RunSummaryDto {
  readonly id: string;
  readonly manuscriptVersionId: string;
  readonly manuscriptName: string;
  readonly modelId: string;
  readonly status: RunStatus;
  readonly startedAt: string;
  readonly finishedAt: string | null;
}

/** 状態別件数（仕様 9「完了した検査単位数と再確認件数」）。5 状態すべてを 0 でも持つ。 */
export type UnitStatusCounts = { readonly [status in UnitStatus]: number };

export interface ProgressDto {
  readonly checkUnits: UnitStatusCounts;
  readonly recheckUnits: UnitStatusCounts;
}

export interface RunTargetDto {
  readonly id: string;
  readonly targetIndex: number;
  readonly target: Range;
  readonly contextBefore: Range | null;
  readonly contextAfter: Range | null;
  readonly input: Range;
  readonly paragraphIds: readonly number[];
}

export interface RunDetailDto {
  readonly run: RunDto;
  readonly progress: ProgressDto;
  readonly targets: readonly RunTargetDto[];
}

export interface UnitFailureDto {
  readonly reason: FailureReason;
  readonly message: string;
  readonly finishReason: string | null;
  readonly origin: "ensure-loaded" | "chat" | "local";
}

export interface CheckUnitDto {
  readonly id: string;
  readonly targetId: string;
  readonly targetIndex: number;
  readonly perspective: Perspective;
  readonly status: UnitStatus;
  readonly attempts: number;
  readonly failure: UnitFailureDto | null;
  readonly pendingNote: string | null;
  readonly elapsedMs: number | null;
  readonly startedAt: string | null;
  readonly finishedAt: string | null;
}

export interface RecheckUnitDto {
  readonly id: string;
  readonly findingId: string;
  readonly inputRange: Range | null;
  readonly status: UnitStatus;
  readonly notApplicableReason: RecheckNotApplicableReason | null;
  readonly attempts: number;
  readonly failure: UnitFailureDto | null;
  readonly pendingNote: string | null;
  readonly verdict: RecheckVerdict | null;
  readonly reasonKind: RecheckReasonKind | null;
  readonly reason: string | null;
  readonly suggestionValid: boolean | null;
  readonly elapsedMs: number | null;
  readonly startedAt: string | null;
  readonly finishedAt: string | null;
}

export interface RunUnitsDto {
  readonly checkUnits: readonly CheckUnitDto[];
  readonly recheckUnits: readonly RecheckUnitDto[];
}

export interface RecoveryDto {
  readonly blocked: boolean;
  readonly runIds: readonly string[];
}

export interface FindingReasonDto {
  readonly candidateId: string;
  readonly perspective: Perspective;
  readonly reason: string;
}

/** 指摘に付く再確認の要約。`RecheckUnitDto` から usage・時刻を除いたもの。 */
export interface RecheckSummaryDto {
  readonly id: string;
  readonly status: UnitStatus;
  readonly notApplicableReason: RecheckNotApplicableReason | null;
  readonly verdict: RecheckVerdict | null;
  readonly reasonKind: RecheckReasonKind | null;
  readonly reason: string | null;
  readonly suggestionValid: boolean | null;
  readonly failure: UnitFailureDto | null;
}

export interface JudgmentDto {
  readonly findingId: string;
  readonly status: JudgmentStatus;
  readonly note: string | null;
  readonly updatedAt: string;
}

export interface FindingDto {
  readonly id: string;
  readonly runId: string;
  readonly targetId: string;
  readonly locateStatus: FindingLocateStatus;
  readonly range: Range | null;
  readonly paragraphId: number;
  readonly quote: string;
  readonly suggestion: string | null;
  readonly category: FindingCategory;
  readonly initialVerdict: InitialVerdict;
  readonly suppression: { readonly word: string; readonly ruleVersion: string } | null;
  readonly reasons: readonly FindingReasonDto[];
  /** 再確認単位が無ければ null（再確認を無効にした実行では起票されない）。 */
  readonly recheck: RecheckSummaryDto | null;
  /** 常に非 null。指摘の作成と同時に `undecided` の行が作られる（PR8 決定 5）。行が無ければ DB 不整合として 500。 */
  readonly judgment: JudgmentDto;
  readonly createdAt: string;
}

export interface CandidateDto {
  readonly id: string;
  readonly checkUnitId: string;
  readonly perspective: Perspective;
  readonly candidateIndex: number;
  readonly llm: LlmFinding;
  readonly locateStatus: CandidateLocateStatus;
  readonly range: Range | null;
}

export interface DiagnosticDto {
  readonly candidateId: string;
  readonly quote: string;
  readonly reason: LocateFailureReason;
  readonly searchRange: Range;
  readonly exactMatches: readonly Range[];
  readonly transformVersion: string | null;
  readonly transformCandidates: readonly DiagnosticCandidate[] | null;
  readonly omitted: number | null;
  readonly tied: boolean | null;
}

/** 指摘の詳細。一覧の項目に元候補と位置診断を足す（決定 15）。 */
export interface FindingDetailDto extends FindingDto {
  readonly candidates: readonly CandidateDto[];
  readonly diagnostics: readonly DiagnosticDto[];
}
```

`FindingDto.recheck` の対応は `recheck_units.finding_id` の一意制約（1 指摘に 1 件）による。
`CandidateDto.perspective` は候補の行には無いので `check_units` から導く（PR8 決定 19）。
`ProgressDto` は `listCheckUnits` / `listRecheckUnits` を数えるだけで、SQL の集計は作らない（件数が小さい）。

## 要求（`shared/src/api/requests.ts`）

zod 4 で書き、`z.infer` した型を同じファイルから export する。**構文的な検証だけ**を zod で行い、
意味的な検証（分割設定の整合、再試行対象の妥当性）は既存の関数（`validateChunkSettings`、
`retryFailedUnits`）に任せて、その例外を 400 に写す（決定 4）。

```ts
export const putConnectionRequestSchema = z.object({
  endpointUrl: z.string().min(1),
  /** 省略＝維持、null＝消去、文字列＝設定（空白のみは消去と同じ。`parseLmStudioApiKey` に合わせる）。 */
  apiKey: z.string().nullable().optional(),
});

export const connectionCheckRequestSchema = z.object({
  modelId: z.string().min(1).optional(),
});

export const createManuscriptRequestSchema = z.object({
  name: z.string().min(1).max(200),
  body: z.string(),                         // 空は zod でなく empty-body で 400 にする（決定 14）
});

// z.int() は zod 4 で「安全な整数」（Number.isSafeInteger）に限る。CLI の parseIntegerOption と同じ範囲。
export const generationSettingsRequestSchema = z.object({
  maxTokens: z.int().min(1),                 // CLI --max-tokens と同じ：1 以上の安全な整数
  temperature: z.number(),                   // zod 4 の z.number() は NaN・±Infinity を通さない
  seed: z.int().min(0).optional(),           // CLI --seed と同じ：0 以上の安全な整数
  reasoningEffort: z.enum(["none", "low", "medium", "high"]).optional(),
});

export const chunkSettingsRequestSchema = z.object({
  targetGraphemes: z.int(),
  contextGraphemes: z.int(),
  recheckContextGraphemes: z.int(),
  roundingTolerance: z.number(),
  maxInputGraphemes: z.int(),
});                                          // 範囲・整合はハンドラーが validateChunkSettings で見る（決定 14）

export const startRunRequestSchema = z.object({
  startOperationId: z.string().min(1).max(200),
  manuscriptVersionId: z.string().min(1),
  modelId: z.string().min(1),
  generation: generationSettingsRequestSchema,
  chunkSettings: chunkSettingsRequestSchema,
  timeouts: z.object({
    checkMs: z.int().min(1).max(MAX_TIMEOUT_MS),
    recheckMs: z.int().min(1).max(MAX_TIMEOUT_MS),
  }),
  perspectives: z.array(z.enum(["typo", "naturalness"])).min(1),   // 重複はハンドラーで除く
  recheckEnabled: z.boolean(),
  allowedWordsRaw: z.string(),
});

export const retryFailedRequestSchema = z.object({
  /** 省略＝全件。空配列は誤り（PR9b 決定 36）なので min(1)。 */
  unitIds: z.array(z.string().min(1)).min(1).optional(),
});

export const confirmRecoveryRequestSchema = z.object({ runId: z.string().min(1) });

export const putJudgmentRequestSchema = z.object({
  status: z.enum(["undecided", "adopt-planned", "rejected", "held"]),
  note: z.string().max(2000).nullable().optional(),   // 省略＝null
});
```

`MAX_TIMEOUT_MS`（`2_147_483_647`）は現在 `server/src/config.ts` にある。`shared/src/api/defaults.ts` へ移し、
`config.ts` はそれを import する（2 か所に書かない）。

## SSE（`shared/src/api/events.ts`）

決定 12 を参照。

```ts
export type RunEventDto =
  | { readonly type: "target-planned"; readonly targetIndex: number; readonly target: Range; readonly input: Range }
  | { readonly type: "check-started"; readonly targetIndex: number; readonly perspective: Perspective }
  | { readonly type: "check-finished"; readonly targetIndex: number; readonly perspective: Perspective; readonly status: UnitStatus }
  | { readonly type: "target-merged"; readonly targetIndex: number; readonly findingCount: number }
  | { readonly type: "recheck-started"; readonly findingId: string }
  | {
      readonly type: "recheck-finished";
      readonly findingId: string;
      readonly status: UnitStatus;
      readonly notApplicableReason: RecheckNotApplicableReason | null;
    }
  | { readonly type: "generation-slow"; readonly unitId: string; readonly elapsedMs: number }
  | { readonly type: "save-rolled-back"; readonly unitId: string; readonly kind: "check" | "recheck" }
  | { readonly type: "stop-requested" }
  | { readonly type: "run-settled"; readonly status: RunStatus; readonly stopReason: RunStopReason | null };
```

`RunEvent`（server 内部）から `RunEventDto` への射影は `server/src/api/events.ts` に置く。
`check-finished` は `result.status` をそのまま写す。`recheck-finished` は `RecheckResult` の状態が
`UnitStatus` と一致しないので写像する：`disabled` → `not-applicable` / `disabled`、`suppressed` →
`not-applicable` / `suppressed`、`pending` / `failed` / `done` はそのまま（`notApplicableReason: null`）。`run-finished`（`PipelineEvent`）はオーケストレーターが出さないので
写像に含めない（PR9b 決定 37）。

## 設計上の決定

### 決定 1：薄いハンドラーだけを置き、サービス層を作らない

ハンドラーは「zod 検証 → オーケストレーター／リポジトリ呼び出し → DTO 射影 → 状態コード」だけを行う。
オーケストレーターがすでにユースケース層であり、リポジトリ呼び出しも 1〜2 行なので、間にもう 1 層置いても
素通しになる。唯一ハンドラーに収まらない `GET /api/runs/:id` と `units` の組み立ては `api/run-view.ts` に
関数として切り出す（HTTP と無関係なので `app.request` を使わずにテストできる）。

**代案（採らない）**：`server/src/service/` にユースケース関数を並べる。将来ハンドラーが太ったときに
切り出せばよく、今は YAGNI。

### 決定 2：型は `shared` に置き、状態語彙を server から移す

PR11 は「型は `shared` から」（ロードマップ）。DTO は `RunStatus` / `UnitStatus` / `JudgmentStatus` /
`RunStopReason` / `RecheckNotApplicableReason` / `CandidateLocateStatus` / `FindingLocateStatus` /
`ReasoningEffort` を参照するが、これらは現在 server にある。web が `@shuten/server` の型を import すると
`node:*` や `better-sqlite3` の型解決が web の tsconfig に入り込むので、**文字列 union である状態語彙を
`shared/src/run/` へ移し、server は再エクスポートに変える**。前例は `shared/src/run/failure-reason.ts`。

移すのは定数配列と型だけ。遷移表（`run/state.ts`）や意味付けは server に残す。

**代案（採らない）**：Hono RPC（`hc`）。web がサーバーのルート型に直接依存し、ロードマップの
「fetch の薄い包み」と合わない。

### 決定 3：公開 DTO への射影は 1 か所、漏えい検査を 1 本置く

`server/src/api/dto.ts` にレコード → DTO の射影関数を並べる（`toRunDto` など）。`RunRecord` の
`endpointUrl` はここで落ちる。ハンドラーはレコードをそのまま `c.json` に渡さない。

テスト A0（後述）が全エンドポイントの応答・エラー本文・SSE ペイロードを JSON 化し、番兵の接続先 URL
（`http://sentinel.invalid:9`）が現れないことを確認する。**例外は `GET/PUT /api/settings/connection` の
正常応答だけ**（仕様 5.1 が接続先 URL の表示を求める唯一の場所）で、その 2 つも `endpointUrl` 以外の
項目には出ないことを見る。API キーの番兵は例外なく全応答で見る。**新しいエンドポイントを足すときは
A0 に加える**（本書のテスト節に明記）。

### 決定 4：エラーは 1 形式、写像は 1 か所

`api/errors.ts` に `ApiError(status, code, message)` を置き、`app.onError` で `{ error: { code, message } }` に
写す。既知の例外の写像：

| 発生源 | 状態 | `code` |
| --- | --- | --- |
| JSON として読めない本文、`Content-Type` 不一致 | 400 | `invalid-json`（`c.req.json()` の例外を写す。500 にしない） |
| zod 検証失敗 | 400 | `validation`（メッセージは不正なパス名の列挙だけ。値は入れない） |
| multipart の項目の型違い（`file` が文字列、`name` が `File`）、同名項目の重複 | 400 | `validation` |
| `MalformedBodyError`（孤立サロゲート。PR8 決定 17） | 400 | `malformed-body` |
| `Utf8DecodeError` | 400 | `invalid-utf8` |
| 空本文 | 400 | `empty-body` |
| `InvalidChunkSettingsError` | 400 | `invalid-chunk-settings` |
| `RetryTargetError` | 400 | `invalid-retry-target` |
| 対象なし | 404 | `not-found` |
| 再開・再試行の拒否（決定 10） | 409 | `run-rejected-<rejectReason>` |
| `stopRun` の `accepted: false` | 409 | `run-not-active` |
| 実行中の接続設定変更（決定 5） | 409 | `runs-active` |
| その他 | 500 | `internal`（定型文のみ） |

`message` には ID だけを入れ、URL・API キー・原稿の断片を入れない。500 のときサーバーのログには
例外のクラス名とスタックを出すが、`LmStudioError` の `message` は接続先を含みうるので**ログにも出さない**
（PR9 決定 14 の方針を HTTP 層でも維持）。

### 決定 5：接続先 URL は `settings` 表、API キーはメモリ。実行中は変更を拒否する

仕様 5.1 は UI から接続先と API キーを設定できることを求め、ロードマップは「UI 側の値は DB に保存」。

- **URL** は `settings` 表（`key` = `lm_studio_url`）に保存し、起動時に行があれば環境変数より優先する。
  保存済みの値が `parseLmStudioUrl` を通らない（DB を手で編集した）場合は環境変数と同じ扱いで、URL を
  含まない定型文を出して非ゼロ終了する（黙って環境変数に戻さない）。
- **API キー**は保存しない。`PUT` で受け取った値をプロセスのメモリに持ち、再起動後は環境変数
  `SHUTEN_LM_STUDIO_API_KEY` の値に戻る。ローカルの OpenAI 互換サーバーは通常キーなしで使うので、
  「再起動のたびに再入力」が実際に起きるのは認証が必要な環境だけ。その環境では環境変数で渡せる。
- `GET` は `hasApiKey: boolean` だけを返す。
- **検査実行が走っている間（`orchestrator.activeRunIds()` が空でない）は `PUT` を 409 で拒否**し、
  `ConnectionManager.update` を呼ばない。走っている実行の接続先が途中で変わる事故を防ぐ。

`settings` 表は `key TEXT PRIMARY KEY, value TEXT NOT NULL, updated_at INTEGER NOT NULL`。PR10 で使う鍵は
1 つだが、表の形は将来の鍵にも使える汎用のままにする（列を足すより安い）。

**代案（採らない）**：キーも DB に平文保存。ユーザーの判断で保存しない。

### 決定 6：オーケストレーターは「接続の供給元」から実行ごとに 1 回読む

`OrchestratorDeps` の `client` と `endpointUrl` を 1 つの `ConnectionSource` に置き換える。

```ts
export interface ConnectionSnapshot {
  readonly client: LmStudioClient;
  readonly endpointUrl: string;
}
export interface ConnectionSource {
  /** 現在の接続。開始・再開・再試行の時点で 1 回だけ読み、その実行に固定する。 */
  current(): ConnectionSnapshot;
}
// OrchestratorDeps: client / endpointUrl → connection: ConnectionSource
```

`startRun` / `resumeRun` / `retryFailedUnits` は `deps.connection.current()` を 1 回呼び、レジストリの
実行エントリに `client` を、`runs.endpoint_url` に `endpointUrl` を入れる。**走っている実行は開始時に
読んだクライアントを持ち続ける**ので、「実行中に接続先が変わらない」がオーケストレーター側で閉じる。
決定 5 の 409 はその上の二重の防御（利用者に「今は変えられない」と伝えるため）。

`ConnectionManager`（`server/src/connection.ts`）が `ConnectionSource` の実体で、`describe()` と
`update()` を足す。`update` は `parseLmStudioUrl` で検証し、`settings` に書き、新しい `LmStudioClient` を
作って古いものを `close()` する。

既存のオーケストレーターのテストは `client` を渡している箇所を固定値の `ConnectionSource`
（`fixedConnection(client, url)` ヘルパー）に変える機械的な修正になる。

### 決定 7：実行中判定は `activeRunIds()` で公開する

`Orchestrator` に `activeRunIds(): readonly string[]`（レジストリの鍵）を足す。決定 5 の 409 判定に使う。
「走っているか」の正本はレジストリであり、DB の `status = running` ではない（起動時に残った `running` 行は
`reconcileOnStartup` が終端化しているので一致するが、判断の根拠を 1 つにする）。

### 決定 8：接続確認はモデル一覧とロード状態の確認まで。試験生成は送らない

`POST /api/settings/connection/check` は**現在の接続**で行う（URL を試してから保存する経路は作らない。
UI は `PUT` → `check` の順に呼ぶ）。

1. `listModels()`。失敗 → `{ reachable: false, error: { reason, message }, models: [], model: null }`。
   `error.message` は **`reason` ごとの定型文**で、例外の `message` をそのまま入れない
   （`LmStudioError.message` は接続先を含みうる。決定 4）。
2. 到達できたら `models` を返す。
3. 要求に `modelId` があれば一覧から完全一致で探し、`found` / `state` / `loaded` を返す。

**試験生成は送らない。** ロードマップの PR10 節は「小さな生成を区別して返す」としていたが、
接続確認の生成がタイムアウトすると LM Studio 側で生成が続いている可能性が残る（PR9 の「生成終了未確認」）。
実行の生成なら executor が `halt` を立てて復旧ゲートを閉じ、`runs` の行が「何を確認すべきか」を表すが、
接続確認は実行の行を持たないので、ゲートを閉じても復旧確認の対象を表現できず、閉じなければ
不変条件「生成終了を確認できなければ後続生成を送らない」に反する。永続化できる操作 ID・ゲートへの登録・
起動時照合まで作れば解決するが、接続確認のためにその機構を持ち込む価値はない。仕様 5.1 の
「接続確認」と不変条件「接続確認では先に `GET /api/v0/models` で `state` を確認する」は 1〜3 で満たす。
モデルとの実際の相性は、最初の検査実行そのものが確かめる（開始直後の `check-finished` / `run-settled`）。

これにより接続確認はキューも復旧ゲートも使わない。`PUT /api/settings/connection` と確認が重なっても、
`listModels` は古いクライアントで完了し（`Agent.close()` は進行中の要求を流す。決定 19）、競合しない。
ロードマップの PR10 節を更新する。

### 決定 9：設定の初期値は `shared` の定数にし、エンドポイントにしない

`shared/src/api/defaults.ts` に `RUN_SETTINGS_DEFAULTS` を置く。web も server も CLI も import できるので、
`GET /api/settings/defaults` は作らない。

```ts
export const MAX_TIMEOUT_MS = 2_147_483_647;
export const RUN_SETTINGS_DEFAULTS = {
  generation: { maxTokens: 16_000, temperature: 0, reasoningEffort: "none" },
  chunkSettings: {
    targetGraphemes: 1_500, contextGraphemes: 1_000, recheckContextGraphemes: 3_000,
    roundingTolerance: 0.2, maxInputGraphemes: 12_000,
  },
  timeouts: { checkMs: 300_000, recheckMs: 300_000 },
  perspectives: ["typo", "naturalness"],
  recheckEnabled: true,
} as const;
```

値は CLI の `DEFAULTS`（`cli/src/args.ts`）と同じ。CLI の `DEFAULTS` はこの定数を参照するよう寄せ、
2 か所に書かない。`reasoningEffort: "none"` は決定記録 0003（2026-09-09）の暫定方針による。

### 決定 10：再開・再試行の拒否理由を判別子で返す

`RunLaunchResult` に `rejectReason` を足す（PR9b の持ち越し「拒否理由の判別子」）。

```ts
export type RunRejectReason =
  | "running"        // 走っているループがある（PR9b O11。done はそのループのもの）
  | "settings"       // stop_reason が settings（PR9b 決定 36）
  | "stale-version"  // 版が変わった（PR9b 決定 45-1）
  | "status";        // その状態からは再開・再試行できない（completed など）
export interface RunLaunchResult extends StartRunResult {
  readonly accepted: boolean;
  readonly rejectReason: RunRejectReason | null;   // accepted なら null
}
```

HTTP は `accepted: false` を 409、`code` を `run-rejected-<rejectReason>` に写すだけで、`settings` 判定や
`hasStaleVersions` を HTTP 層に書き写さない。実行が存在しない場合はオーケストレーターが `Error` を投げる
現状を変えず、ハンドラーが先に `findRun` して 404 にする（同一プロセスなので競合しない）。
`startRun` の原稿版なし（`Error`）も同様にハンドラーが `findManuscriptVersion` で先に弾く。

### 決定 11：復旧確認は API が復旧ゲートを直接使う

「復旧を確認した」操作（PR9b 持ち越し）は `recoveryGate.unblock(runId)` を呼ぶだけなので、
オーケストレーターにメソッドを足さず `api/recovery.ts` がゲートを直接使う。PR9b 決定 24 がゲートを
`index.ts` に置いたのはこのためである。`GET /api/recovery` も `gate.blocked` / `gate.blockedRunIds` を
読むだけ。

`POST /api/recovery/confirm` は実行の状態を変えない（`recovery-waiting` のまま）。再開は別に
`POST /api/runs/:id/resume` で行う。**版が変わった `recovery-waiting` の実行は再開できず終端化もされない**ので、
`reconcileOnStartup` が起動のたびにゲートを閉じ直し、利用者は起動のたびに確認操作をすることになる。
本 PR はそれを「拒否されるだけの再開を試す」から「専用の操作」にするだけで、根本（stale な実行を
終端化する手段）は持ち越す。PR12 はこの操作を「再起動のたびに要る」ものとして表示する。存在しない `runId` を渡されてもゲートの `unblock` は無害なので、
404 にはせず 200 で現在のゲート状態を返す。

### 決定 12：SSE は射影した `RunEventDto` を流し、再送しない

内部の `RunEvent` はそのまま流さない。`check-finished` の `CheckUnitResult` は内部の結果型を丸ごと含み、
将来の変更が線上の契約に直結する。線上には「何が・どうなったか」だけを流し、内容はクライアントが
API で取り直す（不変条件「SSE は通知手段、正本は DB」の素直な形）。

購読の規則（`api/events.ts`。`hono/streaming` の `streamSSE`）：

1. 実行が無ければ 404。
2. **先に hub へ購読してから** DB の状態を読む。終端状態（`status !== "running"`）なら合成した
   `run-settled` を 1 件送って閉じる。`running` ならそのまま流す。この順序で「状態確認と購読の間に決着した」取りこぼしを消す
   （JS は単一スレッドなので、購読後に起きた決着は必ず届く）。
3. `run-settled` を転送したら閉じる。
4. 15 秒ごとに `: ping` コメント行を送る（vite proxy や中継の無通信切断対策）。
5. クライアント切断（`stream.onAbort`）で購読を解除するだけ。**実行は止めない。**
6. 再送はしない（`id:` も付けない）。再接続したクライアントは `GET /api/runs/:id` と `findings` で復元する
   （仕様 8.2）。

`event:` は `RunEventDto.type`、`data:` は JSON 1 行。

**書き込みと終了の機構**（`streamSSE` のコールバックは async で、返るまでストリームが開いている）：

- 購読 1 つにつき `{ onEvent, onClose }` を hub に渡す（決定 13）。コールバックは `closed` という Promise を
  作って await し、`onClose` / `run-settled` の転送後 / `onAbort` / 書き込み失敗のいずれかで resolve する。
  resolve でコールバックが返り、ストリーム本体が完了する。
- `writeSSE()` は Promise を返し、reject は同期の `try/catch` では捕まらない。**購読ごとに 1 本の Promise
  chain を持ち、`onEvent` は `chain = chain.then(() => writeSSE(...))` で直列化する**（イベントが連続しても
  順序が保たれる）。chain の reject は購読解除と `closed` の resolve に写し、以後のイベントは捨てる。
- `: ping` も同じ chain に乗せる（`setInterval` は `closed` で止める）。間隔は注入可能にしてテストで短くする。

PR12 への帰結：`stopped` の実行を購読していたクライアントは合成 `run-settled` で閉じられているので、
`resume` / `retry-failed` を呼んだら**購読し直す**。

### 決定 13：event hub は購読者の例外を握りつぶす

`run/event-hub.ts` の `createRunEventHub()`：

```ts
export interface RunEventSubscriber {
  onEvent(event: RunEvent): void;   // 同期。非同期の書き込みは購読者側が chain に乗せる（決定 12）
  onClose(): void;                  // closeAll から。購読者はストリームを完了させる
}
export interface RunEventHub {
  emit(event: RunEvent): void;
  subscribe(runId: string, subscriber: RunEventSubscriber): () => void;   // 戻り値は購読解除
  closeAll(): void;                 // 全購読者の onClose を呼び、購読を空にする
}
```

`index.ts` が `onEvent: hub.emit` で渡す。`emit` はループから同期に呼ばれるので、購読者の例外を捕まえて
捨てる（投げ返すとループ側で PR9b 決定 33 の内部エラーになり、SSE の不具合が検査を止めてしまう）。
`onClose` の例外も同様に捨てる。`closeAll()` は shutdown（決定 18）から呼ぶ。

### 決定 14：201 は作成直後のスナップショット。二重送信は 200。空本文は原稿の保存で拒否

- `POST /api/runs` の 201 は `startRun` が返した `RunRecord` の射影であり、**その後の結末を含まない**。
  `startRun` は生成要求を送らずに同期で返り、ループは次のマイクロタスクから走る（PR9b 決定 24）ので、
  モデル未ロード（`model-not-loaded`）や復旧ゲート閉鎖（`recovery-blocked`）は 201 の時点ではまだ
  `running` で、直後の `run-settled`（SSE）か `GET /api/runs/:id` で `stopped` になる。
  201 の時点で終端状態なのは、開始計画（`planStart`）で同期に判明するものだけ：入力上限超過
  （`stopped` / `settings`）と、ハンドラーの事前検証をすり抜けた設定不正（同じく `stopped` / `settings`）。
  「失敗を指摘ゼロと誤表示しない」は `status` / `stopReason` を落とさないことで守る。
- **分割設定とタイムアウトの検証はハンドラーが `startRun` の前に行う**（`validateChunkSettings` と
  決定 44 の `timeouts` 検証）。`startRun` は `InvalidChunkSettingsError` を例外にせず `stopped` / `settings`
  の実行を**作る**（PR9b の設計。CLI と同じ経路で記録を残すため）ので、事前に弾かないと入力の誤りごとに
  実行の行が増える。事前検証で 400 `invalid-chunk-settings`、`startRun` 側は二重の防御。
- 同じ `startOperationId` の 2 回目は既存の実行を 200 で返す（PR9b 決定 12。409 にしない。連打した
  クライアントが同じ画面へ進めるようにするため）。
- 空本文（`body.length === 0`）は `POST /api/manuscripts` / `upload` で 400 `empty-body`。仕様 5.1
  「空の本文では検査を開始できない」を、保存できない形で守る（`POST /api/runs` は空本文を見ない）。

### 決定 15：絞り込みはサーバーでしない。一覧と詳細を分ける

指摘は原稿 1 万字で高々数百件なので、絞り込み（分類・採否・再確認状態・位置特定失敗）は PR12 が
クライアント側で行う。`GET /api/runs/:id/findings` にクエリパラメーターは付けない。

一覧の `FindingDto` は理由・再確認要約・採否まで含め（一覧画面の絞り込みに要る）、元候補と位置診断は
`GET /api/findings/:id` の `FindingDetailDto` にだけ入れる（詳細パネルでしか使わず、診断は大きい）。
`listFindings` の N+1（PR9b 持ち越し）はここでは直さない（PR12 で件数を見て判断）。

### 決定 16：エクスポートは PR13

ロードマップは PR10 に `GET /api/runs/:id/export` を置いていたが、形式は評価ツール（PR13）と揃えて
決めるほうが二度手間にならない。仕様 8.2 の「保存済み結果の再閲覧」は `GET /api/runs/:id` と
`findings` で満たす。ロードマップの PR10 節・PR13 節を更新する。

### 決定 17：接続断で `failed` になった単位の復旧は 2 段のまま

PR9b 持ち越しのとおり、生成中に応答を受け取れないまま接続が切れた単位は `failed` のまま残り、
復旧には「再開」→「失敗単位の再試行」の 2 段が要る。1 段にするには `UnitFailure` の拡張と
PR9 決定 20 の改訂が要り、HTTP 層と無関係な変更が同じ PR に混ざるので本 PR では広げない。
`GET /api/runs/:id` の `progress.checkUnits.failed` で失敗件数が取れるので、PR12 は
「再開したあと失敗単位を再試行してください」と案内できる。決定 20 の改訂は持ち越しのまま残す。

### 決定 18：graceful shutdown は走っている実行を待たない

`index.ts` で `SIGINT` / `SIGTERM` を受けたら、①`server.close(cb)`（新規受付停止。**await しない**。Node の
`close` は全接続の終了を待つが、SSE は自然には終わらない）→ ②`hub.closeAll()`（全 SSE を閉じる）→
③`server.closeAllConnections()`（残った keep-alive 接続を切る）→ ④`cb` を待つ → ⑤`connection.current().client.close()`
（undici `Agent.close()`。進行中の要求は流す）→ ⑥`close()`（DB）→ `process.exit(0)`。全体に 5 秒の上限を置き、
超えたら `process.exit(1)`。走っている実行は**待たない**。LM Studio 側の生成は止められない前提なので、次回起動の
`reconcileOnStartup` が `backend-restarted` として照合する（PR9 決定 13）。2 回目のシグナルは即終了。

Windows は `SIGTERM` が届かず、コンソールの Ctrl+C（`SIGINT`）だけが対象。その旨を README に書く。

### 決定 19：`LmStudioClient.close()` を足す

PR7 からの持ち越し。undici の `Agent` を `close()` する。`ConnectionManager.update` が古いクライアントを
閉じるときと shutdown で使う。テストのフェイククライアントは no-op でよい。

### 決定 20：ファイル入力は multipart、`ingestUtf8Bytes` だけを通す

`POST /api/manuscripts/upload` は `c.req.parseBody()` で `file`（`File`）と `name`（文字列）を受け取り、
`new Uint8Array(await file.arrayBuffer())` を `ingestUtf8Bytes` に渡す。`Utf8DecodeError` は 400
`invalid-utf8`。CRLF は保持される（バイト列で受けるため。仕様 5.1）。`name` が無ければ `file.name` を使い、
その結果に `createManuscriptRequestSchema` の `name`（1〜200 文字）と同じ制約を適用する。`file` が `File` で
ない、`name` が文字列でない、同名項目が複数ある（`parseBody` が配列を返す）場合は 400 `validation`。Hono にも `@hono/node-server` にも本文サイズの上限は無く、本 PR でも設けない
（仕様 9「上限は実測後に決める」）。

## `index.ts` の配線

```ts
const { db, close } = createDatabase(resolveDatabaseFile(config));
applyMigrations(db);
const connection = createConnectionManager({ db, config });   // settings 表 → 環境変数の順
const queue = createRequestQueue();
const recoveryGate = createRecoveryGate();
const hub = createRunEventHub();
const orchestrator = createOrchestrator({
  db, connection, queue, recoveryGate,
  recoveryConfirmMs: config.recoveryConfirmMs,
  onEvent: hub.emit,
});
orchestrator.reconcileOnStartup();
const app = createApp(config, { db, connection, recoveryGate, orchestrator, hub });   // queue は API が使わない
const server = serve({ fetch: app.fetch, hostname: config.host, port: config.port }, ...);
// shutdown（決定 18）
```

`createApp(config, deps)` は `createApiRouter(deps)` を `/api` にマウントし、静的配信は現状のまま。
`app.test.ts` の `GET /api/health` は残す（`deps` なしでも health だけ動く形にはせず、テストではフェイクの
deps を渡す）。

## 仕様・受け入れ条件との対応

| 受け入れ条件（11 節） | 本 PR で満たすもの |
| --- | --- |
| 2 項：貼り付けと UTF-8 テキスト読み込みの両方から検査できる | `POST /api/manuscripts` / `upload` → `POST /api/runs` |
| 12 項：採用予定・却下・保留を変更して保存でき、本文は変化しない | `PUT /api/findings/:id/judgment`。本文を書く経路が無い |
| 13 項：ブラウザの再読み込み後も結果と判断が保持される | `GET /api/runs/:id` / `findings` が DB から返す |
| 16 項：複数タブ・複数原稿でも単一キュー | プロセス内 1 個のキューを API と接続確認が共有する |

画面がまだ無いので、利用者が実際に触って確認できるのは PR11・PR12 の完了後。

## PR11・PR12・PR13 への持ち越し

- 画面（PR11・PR12）。API クライアントは `shared/src/api/` の型と zod を使う。
- `listFindings` の N+1（PR12 で件数を見て判断）。
- エクスポート（PR13。決定 16）。
- 接続断で `failed` になった単位を `pending` に戻す（決定 17。PR9 決定 20 の改訂を伴う。別 PR）。
- アップロードのサイズ上限（仕様 9。実測後）。
- `recoveryConfirmMs = 120000` の見直し（PR13）。
- 版が変わった `recovery-waiting` の実行を終端化する手段（決定 11。無いと起動のたびに復旧確認が要る）。

## テスト

すべて `app.request` で通す（ハンドラーテスト）。DB は `createDatabase(":memory:")`、LM Studio は
オーケストレーターのテストと同じフェイククライアント。ID の接頭辞は A（API）。

### A0：漏えい検査（`api/leak.test.ts`）

番兵の接続先 URL `http://sentinel.invalid:9` と番兵 API キー `sentinel-api-key` で起動し、
本書の全エンドポイント（正常・エラー・SSE）を呼び、応答本文・エラー本文・SSE の全行を文字列化して
両番兵が現れないことを確認する。URL の番兵だけは `GET/PUT /api/settings/connection` の正常応答の
`endpointUrl` に限って許す（決定 3）。**エンドポイントを足すときはここに加える。**
エラーメッセージに原稿の番兵断片（`SENTINEL-MANUSCRIPT-FRAGMENT`）が出ないことも同じテストで見る
（指摘の `quote` や原稿の `body` に原稿が入るのは正常なので、正常応答の本文は対象外）。

### A1：接続設定（`api/settings.test.ts`）

- `GET` が `endpointUrl` と `hasApiKey` を返し、キー本体を返さない
- `PUT` で `/v1` 付き・非 http の URL が 400 `validation`
- `PUT` が `settings` 表に書き、再起動（`createConnectionManager` の作り直し）で行が環境変数より優先される
- `PUT` の `apiKey` 省略で維持、`null` で消去、文字列で設定（`hasApiKey` で観測）
- 実行中の `PUT` が 409 `runs-active` で、`update` が呼ばれない
- `PUT` 後の `startRun` が新しいクライアントを使い、`PUT` 前に走っていた実行は古いクライアントのまま
  （フェイククライアントを 2 つ用意し、`chat` の呼び出し先で判別）
- `PUT` が古いクライアントの `close()` を呼ぶ
- 接続確認：到達不能（`reachable: false`、`error.message` が定型文で URL なし）／モデル未指定
  （`model: null`）／未発見（`found: false`）／未ロード（`loaded: false`）／ロード済み（`loaded: true`）。
  **どの分岐でも `chat` が呼ばれない**

### A2：原稿（`api/manuscripts.test.ts`）

- 貼り付け：201 で `body` が無加工（CRLF・BOM 付き文字列をそのまま。貼り付けでは BOM も除外しない）
- 空本文 400 `empty-body`、孤立サロゲート 400 `malformed-body`、`name` 欠落 400 `validation`
- アップロード：BOM 付き UTF-8 バイト列の BOM だけ除外、CRLF 保持、不正 UTF-8 400 `invalid-utf8`
- アップロードの `name`：省略で `file.name`、201 文字で 400 `validation`。`file` が文字列／`name` が `File`／
  同名項目の重複で 400 `validation`
- `GET` の 200 / 404

### A3：実行（`api/runs.test.ts`）

- `POST` 201 と `RunDto` の形（`endpointUrl` / `startOperationId` を含まない）
- 同じ `startOperationId` で 2 回 → 200、同じ ID、実行は 1 本（`chat` の回数で確認）
- 分割設定不正 400 `invalid-chunk-settings` で**実行の行が作られない**、原稿版なし 404、観点の重複を除いて開始
- モデル未ロード：201 の時点で `status: "running"`、`done` の後の `GET` で `stopped` / `model-not-loaded`
- 入力上限超過：201 の時点で `stopped` / `settings`（開始計画で同期に判明する）
- `GET /api/runs` が原稿名付きで降順
- `GET /api/runs/:id` の `progress` が 5 状態すべてを持ち、合計が単位数に一致。`targets` が保存済み範囲
- `GET /api/runs/:id/units` の `targetIndex` が対応する検査対象と一致
- `stop` 202 → 決着後に `status: "stopped"`。レジストリに無い実行は 409 `run-not-active`
- `resume` 202（`stopped` から）。`completed` は 409 `run-rejected-status`、`settings` は
  `run-rejected-settings`、版が変わった実行は `run-rejected-stale-version`、走っている実行は
  `run-rejected-running`
- `retry-failed` 202。空配列 400 `validation`、`input-too-long` の単位を指定 400 `invalid-retry-target`

### A4：復旧（`api/recovery.test.ts`）

- `GET` がゲートの状態を返す
- `confirm` でゲートが開き、以後の `startRun` が `recovery-blocked` にならない。存在しない `runId` でも 200

### A5：指摘と採否（`api/findings.test.ts`）

- 一覧：理由（観点付き）、`recheck`（起票あり／なし）、`judgment`（未判断は `status: "undecided"`）、抑制の有無
- `judgments` の行を消した指摘の一覧が 500 `internal`（正常値に丸めない）
- 詳細：元候補（観点付き）と位置診断（位置特定失敗の指摘で非空）。404
- 採否：`PUT` で作成・更新、`note` 省略で null、不正な `status` 400、本文（原稿版）が変わらない、
  再確認の結果が採否を上書きしない（採否設定後に再確認を決着させても `judgment` が残る）

### A6：SSE（`api/events.test.ts`）

- 購読中に応答を abort → `done` が解決し、フェイククライアントの `chat` 回数が切断後も増える
- 終端状態の実行を購読 → `run-settled` 1 件で閉じる
- 購読後に決着 → `run-settled` が届いて閉じる
- 存在しない実行 → 404
- `check-finished` の `data` が `CheckUnitResult` の中身（`usage` など）を含まない
- 連続イベント（同期に 3 件 `emit`）が送った順で届く
- `writeSSE` が reject する（閉じた応答へ書く）と購読が解除され、ストリームが完了し、以後 `emit` しても
  書き込みが試みられない
- `closeAll()` で待機中の SSE 応答が完了する（`res.body` の読み取りが終端に達する）
- `: ping` が注入した間隔で届く
- hub：購読者が投げても他の購読者に届き、`emit` が投げない。`closeAll` 後は届かず、`onClose` が各 1 回
- 前提の確認を最初に行う：`app.request(path, { signal })` の abort が `stream.onAbort` に届くこと
  （届かなければ `@hono/node-server` 経由の実 HTTP でテストする）

### A7：エラー写像と静的配信（`api/errors.test.ts`、`app.test.ts`）

- 未知の例外が 500 `internal` で定型文のみ
- 壊れた JSON・`Content-Type: text/plain` の本文が 400 `invalid-json`（500 にならない）
- zod 失敗の `message` にパス名だけが入り値が入らない
- `/api/health` と `/api/*` 以外の静的配信が共存する

### A8：shared の状態語彙（`shared/src/run/*.test.ts`）

- 移した定数が server の再エクスポートと同一（参照の一致）

### 既存テストの改修

- `orchestrator.*.test.ts`、`loop.test.ts`：`client` / `endpointUrl` を `connection: fixedConnection(...)` に
- `orchestrator.stop.test.ts` 等：`RunLaunchResult` の `rejectReason` を各拒否ケースで検査
- `config.test.ts`：`MAX_TIMEOUT_MS` の import 元の変更
- `cli/src/args.test.ts`：`DEFAULTS` が `RUN_SETTINGS_DEFAULTS` と一致

## Windows

OS 固有の処理はシグナル（決定 18）だけ。動作確認は CI（windows-latest）による。ローカルの Windows では
未確認である旨を PR に書く。
